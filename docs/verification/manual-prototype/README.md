# Manual prototype verification artifacts

2026-09-14. Generated by `tests/browser/studio.spec.ts` against an isolated production server and temporary private database.

- `desktop-pattern.png` and `mobile-pattern.png`: real CPU pattern geometry from explicitly synthetic body values; not a simulated garment or physical-fit result.
- `export.png`: saved-revision export controls and authenticated download links.
- `sample-tech-pack.pdf`: incomplete original-garment draft with one material and no pattern; unknown data stays unknown.
- `populated-tech-pack.pdf`: UI-authored material/POM/construction/callout/review data with actual CPU panels and three committed pattern artifacts. The two uploaded color swatches are synthetic reference-plumbing fixtures, explicitly not garment flats.

The PDFs carry revision/snapshot identities and disclosure limitations. Pattern files remain printable references without physical calibration or fit evidence. Random IDs/timestamps belong only to test-local records; the underlying temporary database is removed after each test. No personal photographs, wearer measurements or real maker identity are included.

Verification: 64 API/export cases, 16 engine/resource-limit cases, and 10 browser cases pass. Run `bun run test`, `bun run test:engine`, and `bun run test:browser` separately; Playwright cases must not be loaded by Bun's built-in test discovery. TypeScript, production build, documentation links and whitespace checks also pass. See [implementation review](../../reviews/manual-prototype.md) for remaining gates.
