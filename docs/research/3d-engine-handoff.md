# 3D engine handoff

**September 25, 2026 · Research continuation · Not deployment-ready**

Current evidence: [cable integration decomposition](3d-solver-feasibility.md#cable-integration-decomposition--2026-09-25) preserves the complete original 129-record history in both fixed128 arms, including stopped trial 129. All actual native children intentionally exit **86**, all outer/container launches and the saved comparator exit zero, inputs remain unchanged and cleanup completes. Eight scopes and their direct timing edges account for **15.792775042 self CPU seconds / 35.056%** of the **45.050299078-second** operation.

The largest selected cost is smooth response work excluding radial moments: **8.725839665 seconds / 19.369%**. This residual combines composition, tolerance calculation and interval arithmetic. Response radial moments take **3.841692199 seconds / 8.528%**, and functional construction takes **2.385385814 seconds / 5.295%**. Geometry also executes outside response intervals and is attributed through parent–child edges. Boundary-response bounds are never called; their cost remains unmeasured. Both pinned parents pass **28+10+13 controls**, and all **26 saved-data cases** match expectations. Numerical sources are unchanged; the previous **1,610-test** numerical checkpoint remains applicable without a new full suite. The [ledger](3d-cable-integration-profile-results.json) retains the complete evidence and review history. Full 64 ms accuracy, spatial convergence, cuff turning/binding, material/body/full-shirt and private application/release gates remain open. Production is unchanged; nothing is pushed or deployed.

Next run one capped exact interval-contraction investigation inside `_smooth`, with per-call reuse of fresh moment endpoint/sign/denominator data. Require independent exact endpoint and complete-response/work equivalence, bounded integer growth and a material isolated cost witness before full regressions or matched benchmarking; stop if the witness is weak. Separately define a higher-order integrator's stage/velocity, control-work quadrature, contact/path/energy and estimator semantics. The main milestone remains complete same-input temporal accuracy, and changing controller order alone cannot upgrade the current step. Reproduce the measurement ledger with `python3 .planning/solver/package-cable-integration-profile-results-v1.py --check` using retained local artifacts.

Previous implementation: [shared cable polynomial products](3d-solver-feasibility.md#shared-cable-polynomial-products--2026-09-25) reuses exact row-pair products within fresh force evaluations and shortcuts canonical exact-Fraction zero multiplication. Independent objects, ordered rows, fallback behavior and all fresh native checks remain intact. **1,610 full numerical/supervision tests pass in 872.516 seconds**; the 189 focused and eight host checks overlap that total. Ten new methods include independent analytic coefficient oracles and complete native response/fixed-work/imported-parameter-work comparisons.

All six declared 128-call observations preserve complete histories against the prior reference, including stopped trial 129. Actual child exits are **86** by design; all outer launches and the saved comparator exit zero with clean cleanup and unchanged inputs. Independent review confirms paired operation CPU reductions of **1.496%, 3.277% and 3.302%**. Median CPU falls from **45.520797326 to 44.017797852 seconds**, a **3.302%** reduction. Each candidate is lower in its pair; three observations do not establish population confidence, whole-worker speed or full-64-ms feasibility.

Next run one bounded decomposition of fresh cable-response integration on the unchanged workload, separating radial moments, interval assembly, boundary partitioning and remaining functional construction. Use the result to select the next implementation before another unchanged full-budget accuracy attempt. Preserve original physical inputs, complete work/path/native-velocity checks and accuracy/resource criteria. A higher-order time policy would require explicit stage, velocity, control-work and contact/path semantics; changing controller order alone cannot upgrade the current backward-Euler step. The [ledger](3d-cable-shared-products-results.json) retains this checkpoint, and `python3 .planning/solver/package-cable-products-results-v1.py --check` reproduces it from retained local artifacts. Full 64 ms accuracy, original preload/released tail, spatial convergence, actual cuff turning/binding, material/body/full-shirt and private application/release gates remain open. Production is unchanged; nothing is pushed or deployed. Earlier entries retain their then-current priorities.

Previous implementation: [exact cable identity composition](3d-solver-feasibility.md#exact-cable-identity-composition--2026-09-25) avoids polynomial products for exact Fraction identity maps while preserving independent coefficient instances and the prior fallback. **1,600 full numerical/supervision tests pass in 972.878 seconds**, including ten independent methods. The overlapping 179 focused tests include complete response, fixed work and imported parameter-work comparisons against independent binomial and previous-Horner calculations.

All six declared 128-call observations preserve complete raw histories against the prior reference, including stopped trial 129, after only verified run-identity normalization. Actual numerical child exits are **86** by design; all outer launches and the saved comparator exit zero with clean cleanup. Independent saved-result review passes. Timing is mixed: paired operation CPU reductions are **−0.097%, +4.474% and +7.720%**; medians are **52.157667508 seconds baseline** and **50.615135385 seconds candidate**, a **2.957%** reduction. Preserve the adverse pair. No consistent, population-level, whole-worker or full-64-ms speedup is established.

Next review repeated exact polynomial products within fresh cable force construction, especially common vertex-row products repeated across Hessian components, in a new isolated checkout. This is a source-derived candidate, not an established primitive-level bottleneck. Preserve fresh evaluations, exact response/work records, arithmetic/resource limits and complete-history verification. The [ledger](3d-cable-identity-composition-results.json) retains the regression, timing, process and review evidence; `python3 .planning/solver/package-cable-compose-results-v1.py --check` reproduces it from retained local artifacts. Full 64 ms accuracy, original preload/released tail, spatial convergence, real cuff turning/binding, material/body/full-shirt and private application/release gates remain open. Production is unchanged; nothing is pushed or deployed. Earlier entries retain their then-current priorities.

Previous evidence: [isolated-helper callback profiling](3d-solver-feasibility.md#isolated-helper-callback-profiling--2026-09-25) preserves the complete original 128-call history in both arms, including stopped trial 129. Both children intentionally exit **86**, both outer launches and the separate saved comparator exit zero, and independent saved-result review passes. All eleven source-bound counts, CPU/wall accounting and restoration checks pass; each pinned parent passes **28+10+14** diagnostic controls. The **78** serialized-sidecar and **11** fake parent-gate cases also match expectations.

Fresh cable diagnostics takes **7.829210635 CPU seconds / 15.559%** of the **50.319088174-second** operation; contact validation takes **2.220854653 seconds**. Callback bodies account for **99.650%** of the helper's **12.855561316 inclusive seconds**. Its remaining self time is **0.044995375 seconds**, including classification, copying, checks and instrumentation. Explicit control construction and parameter-record calls together use **0.833354040 seconds**; they are not established as the principal target. Family costs include lower-level work inside each body, not all calls to those methods throughout the operation.

Next implement a narrow exact-polynomial optimization inside fresh cable-response evaluation in a new isolated checkout, starting with identity affine composition. Preserve exact rational coefficients, full response/work records, arithmetic budgets and every fresh numerical/native validation. Require independent mathematical/adversarial checks, applicable regressions and matched-history performance evidence, then return to the original incomplete 64 ms study and construction/full-shirt gates. This checkpoint implements no numerical optimization or established speedup.

The [ledger](3d-isolated-helper-results.json) retains source, actual process, control, review and failed-review evidence. `python3 .planning/solver/package-isolated-helper-results-v1.py --check` reproduces it in the isolated study checkout. Numerical/application sources are unchanged; the **1,590-test** baseline remains historical, with no new full suite or deployment. Temporal/spatial accuracy, original preload/released tail, real cuff turning/binding, material/body/full-shirt and private application/release acceptance remain open. Previous entries below retain their then-current priorities.

Previous evidence: [adaptive-validation scope profiling](3d-solver-feasibility.md#adaptive-validation-scope-profiling--2026-09-25) preserves the complete original 128-call history in both arms, including stopped trial 129. Both children intentionally exit **86**, both outer launches and the separate saved comparator exit zero, and all inputs/cleanup/count/restoration checks pass. Each pinned parent passes the unchanged 28 helper/10 phase controls plus seven independent adapter controls. The 64 serialized-sidecar and 11 fake parent-gate cases match expectations.

The eight selected scopes explain **13.838503648 self CPU seconds / 91.187%** of the **15.176035639-second** broad controller/other interval. The isolated helper dominates: **12.977065781 self seconds / 24.676%** of the **52.590812467-second** operation. Its supplied callees perform actual fresh numerical validation; this is not evidence that array copying costs that much. Array equality takes 0.028079375 seconds, JSON equality 0.531880745 seconds and enrichment 0.176306125 self seconds. The remaining broad interval is **1.337531991 seconds**. Inclusive target times overlap and cannot be added.

Next isolate immutable cable-control construction and fresh response/work evaluation inside the helper, retaining contact-validation attribution separately. Optimize only an established expensive operation while preserving fresh native checks, the original physical problem, resource limits and exact histories. Then return to the incomplete full 64 ms accuracy study and the construction/full-shirt gates. This increment is diagnostic evidence, not an optimization or speedup. The [ledger](3d-adaptive-validation-results.json) binds the retained sources, actual processes, controls and failures; `python3 .planning/solver/package-adaptive-validation-results-v1.py --check` reproduces it in its isolated checkout. Numerical/application sources are unchanged. No full suite or deployment ran; the 1,590-test baseline below remains historical evidence.

Previous evidence: [targeted controller scope profiling](3d-solver-feasibility.md#targeted-controller-scope-profiling--2026-09-25) preserves the complete original 128-call history in both arms, including stopped trial 129. Both numerical children intentionally exit **86**; both outer launches and the separately bounded saved comparator exit zero with clean cleanup. Source-bound counts, restoration before export and the 28 helper/10 independent phase controls pass; 63 serialized-sidecar and 11 fake parent-gate cases also match expectations.

The eight measured functions use only **0.511049793 self CPU seconds / 1.019%** of the **50.148306528-second** operation. They explain **3.395%** of its broad controller/other interval, leaving **14.543230151 seconds** unassigned. This does not support optimizing these selected functions as the main cost. Next measure surrounding response/energy revalidation and report enrichment on the same workload; keep every fresh native check, physical criterion, resource bound and complete-history requirement. These are source-derived candidates, not established bottlenecks.

The [controller-scope ledger](3d-controller-scope-results.json) retains declarations, reviewed methods, actual results and failures. `python3 .planning/solver/package-controller-scope-results-v1.py --check` reproduces it from retained local artifacts. Numerical and application sources are unchanged; no new full suite, speedup, full-duration accuracy or garment acceptance is claimed. The 1,590-test checkpoint below remains the numerical baseline. Full 64 ms accuracy, original preload/released tail, spatial convergence, actual construction, material/body/full-shirt and private application/release gates remain open. Nothing is deployed.

Previous evidence: [coordinate admission and artifact inventory](3d-solver-feasibility.md#coordinate-admission-and-artifact-inventory--2026-09-25) now pass **1,590 full numerical/supervision tests in 879.978 seconds**, with 226 overlapping focused tests also passing. Fresh descriptors anchored to the trusted directory repair missed-file observations and iterator interference. The ten-method old-observer control retains its five expected failures; earlier failed regressions and diagnostic masking remain explicit. Coordinate admission also avoids discarded rational allocations while retaining exact geometry and fresh native validation.

All six prospectively ordered 128-call observations match the complete prior raw history, including stopped trial 129, after only run-identity normalization. Each child intentionally exits **86**, each outer process exits zero, and the saved comparator exits zero. Performance is mixed: the three candidate operation CPU reductions are **−1.037%, +0.719% and +3.254%**; medians are **50.278492346 seconds baseline** and **49.917095692 seconds candidate**. The **0.719%** median reduction does not establish a consistent, population-level or whole-worker speedup. Retain the adverse pair.

The [coordinate/inventory ledger](3d-contact-coordinate-admission-results.json) binds regression, negative-control, observer, process, history and timing evidence. `python3 .planning/solver/package-coordinate-admission-results-v3.py --check` reproduces it from the preserved local artifacts. Next isolate unassigned controller/reporting costs on the unchanged fixed workload before another full numerical budget. Full 64 ms accuracy, original preload/released tail, spatial sensitivity, source construction and application gates remain open. No deployment or physical acceptance follows. Earlier entries below preserve their then-current next steps.

Previous evidence: [matched targeted scope profiling](3d-solver-feasibility.md#matched-targeted-scope-profiling--2026-09-25) now completes both 128-call arms within the unchanged bounds. Both numerical children intentionally exit **86**; the separate saved comparator exits zero and verifies complete raw histories against each other and the prior reference, including stopped trial 129, after only unique run-identity normalization. All seven wrapper counts agree. **28 helper controls** pass on the host and in both Linux parents; **52 sidecar cases** and **10 parent-gate cases** pass separately.

The early operation uses **51.620 CPU seconds**. Exact contact construction accounts for **9.324 self CPU seconds / 18.06%**, the largest selected scope; logarithm series account for **1.36%**. Nested inclusive times overlap, and **33.216 seconds** remains outside the selected scopes. This is no whole-worker cost, speedup or accuracy claim. The [scope ledger](3d-scope-profile-results.json) retains process, source/count/timer and review identities; `python3 .planning/solver/package-cached-scope-profile-results-v1.py --check` reproduces it. Preserve all frozen `cached-profile-scopes128-*` inputs/outputs and the prior failed profiler evidence.

Next review a narrow allocation change in a fresh isolated checkout: avoid constructing and discarding exact rational coordinates during strict pure-geometry cache admission. Preserve all type/finiteness, feature and arithmetic-budget checks, per-call native parameter checks, fresh endpoint observations and validation, memory bounds and complete histories. Measure any benefit; the aggregate construction timing does not identify these allocations as its dominant cost. Broader reuse of mutable native buckets needs separate review. This checkpoint implements no numerical optimization and changes no application/production source. Full 64 ms accuracy, original preload/released tail, spatial sensitivity, construction and application gates remain open.

Previous evidence: the [function-level profiling attempt](3d-solver-feasibility.md#failed-function-level-profiling--2026-09-25) fails under its unchanged resource limits. The control reaches 128 calls; the instrumented worker receives the 360-second wall-limit stop after **119 calls**. The traceback places the stop exception inside the profiler clock callback, and summary validation refuses a function-profile report. Actual numerical child exit is **1**, separate from the earlier proposed 86 and outer Docker zero. Cleanup succeeds and both arms pass all **35 helper controls** in the pinned runtime. No matched-history comparator runs and no function-cost conclusion follows.

The [failed-attempt ledger](3d-function-profile-attempt-results.json) retains both runs, the 9,880,380-byte interrupted snapshot, declarations, reviewed methods and actual outcomes. `python3 .planning/solver/package-function-profile-attempt-v1.py --check` reproduces it. Frozen `cached-profile-functions128-*` inputs and outputs remain in ignored `.planning/solver/`; preserve them. Next declare lightweight targeted scope timers for the unchanged 128-call workload, keeping fresh native endpoint validation, exact-history checks and existing resource/accuracy criteria. No concrete optimization has been selected. Numerical/application sources and production remain unchanged; all full-trajectory, construction and application gates remain open.

Previous evidence: [matched worker profiling](3d-solver-feasibility.md#matched-worker-profiling--2026-09-25) passes three-call and 128-call control/instrumented comparisons. Complete raw histories match after only verified run-identity normalization; the first 128 trial/reservation records and all published controller prefixes also match the audited original A history. Each numerical child stops deliberately with actual exit **86**. Both separate saved comparators exit zero. The 128-call operation uses **51.512 process CPU seconds**: **48.33% solver steps**, **23.32% energy transitions**, **28.35% other controller/report work including timer overhead**. Snapshot and publication together use another 1.419 CPU seconds within the worker scope. These are early-workload measurements, with no whole-worker speedup or accuracy claim.

The [profiling ledger](3d-cached-profile-results.json) preserves both pairs and their input/process identities. Local frozen methods and declarations use `cached-profile-three-*` and `cached-profile-early128-*` names in ignored `.planning/solver/`; `python3 .planning/solver/package-cached-profile-results-v1.py --check` reproduces the ledger. Next isolate costs inside solver steps and energy transitions before selecting a change; preserve fresh native validation and exact workload histories. No numerical/application source changed or new full suite ran. Full 64 ms accuracy, preload/released-tail controls, spatial sensitivity, garment operations, full-shirt validation and app/release gates remain open. Earlier entries retain their then-current next steps.

Previous evidence: the [cached adaptive prefix study](3d-solver-feasibility.md#cached-adaptive-prefix-study--2026-09-25) retains **2,318 valid trials**, **808 committed fine intervals** and a **6.7578125 ms** committed prefix under the original A physical inputs and accuracy criteria. The numerical child actually exits **86** at its declared 1,800-second wall limit. All eight separate response/work/path audit chunks pass, covering **82,612 contact leaves** and **9,272 triangle leaves**. A fresh raw-history reduction verifies complete candidate coverage and source/receipt/proof identities; its own actual exit is zero. These saved-evidence results do not complete the 64 ms trajectory, released tail or temporal-accuracy study. The tighter B case is frozen but unexecuted.

The [cached-study ledger](3d-cached-temporal-study-results.json) preserves the 196,410,757-byte history and 56,880,890 bytes of proof identities, actual stopped outcome and original failed evidence. A Docker CLI exit 125 rejected an unsupported address-space flag before checker startup; the corrected offline Linux reduction succeeds with the unchanged reducer's own 6 GiB address-space limit before input processing. Startup/imports and GNU timeout do not share that reducer limit. Independent review covers the command correction and wrapper faults; no new full numerical/application suite or deployment is claimed. The preceding 215-test checkpoint covers byte-identical tested script inputs.

Local declarations and source snapshots are in `.planning/solver/cached-adaptive-a-declaration-v2.json`, `cached-adaptive-a-execution-v2.json` and `cached-study-sources-v2`; results are in `cached-adaptive-a-runs-v2` and `cached-adaptive-a-reduction-v3`. The rejected checker launch remains in `cached-adaptive-a-reduction-v2`. `python3 .planning/solver/package-cached-study-results-v2.py --check` reproduces the ledger from retained local artifacts. Next measure the existing worker with matched fixed-dispatch profiling controls before another full budget; require exact matched numerical histories. Preserve the original preloaded case, released tail and all failed reference comparisons. The entries below retain earlier checkpoints and their then-current next steps.

Previous implementation: [retained trial response/path auditing](3d-solver-feasibility.md#retained-trial-response-and-path-auditing--2026-09-24) now maps every valid raw trial to its true start, retaining discarded alternatives and valid unassessed tails without advancing the committed path. **215 applicable tests pass in 28.893 seconds**, including seventeen independently authored mapping attacks. Two prospectively declared stopped controls preserve the original separated eight-vertex/four-face/two-cell fixture and original A criteria. The extended run retains **128 valid trials**, **42 committed fine intervals**, **86 uncommitted trials** and a **1.890625 ms** committed prefix; separate audits include active contact, complete endpoint response identities, cable/contact work and contact/triangle/hinge paths. Both actual worker exits remain **86**, and neither run establishes full temporal accuracy. The [ledger](3d-temporal-response-results.json) binds all source/context/artifact identities and keeps native/numerical trust limits explicit. The first 24 trial records overlap exactly; do not add the runs as distinct physical states.

Supplemental review on September 25 reproduces shared-law membrane/bending/kinetic work and inactive-control zeros for every valid observation in both histories. Each separately declared review reproduces an unchanged positive control and rejects eleven altered inputs at their intended checks, including consistent endpoint/control substitutions, wrong branch starts and exchanged membrane/kinetic work. Both actual review exits are zero; no solver step is executed. Shared constitutive/native laws and a bounded attack corpus remain explicit limitations. The original denied Docker launch remains failed evidence with outer exit 126, separate from its successful authorized retry.

Local source snapshots, raw histories and proof artifacts for this increment remain in ignored `.planning/solver/response-check-sources-v1`, `response-check-sources-v2` and matching `response-check-runs-v1`/`response-check-runs-v2` directories. Supplemental review inputs are in `response-mutation-reviewer-sources-v1`; declarations and exact commands are in `response-mutations-declaration-v1.json`/`v2.json` and `response-mutations-execution-v1-authorized.json`/`v2.json`. Results are in `response-mutations-runs-v1-authorized` and `response-mutations-runs-v2`; the denied launch remains in `response-mutations-runs-v1`. `python3 .planning/solver/package-response-results-v1.py --check` reproduces the response ledger and verifies required receipt/artifact links. Native replay requires the retained historical reference helpers and pinned offline runtime. Next declare the cached accuracy study with the original force/state goals, complete costs, source inputs, preloaded regression and released tail preserved. Source garment construction and application acceptance remain open.

Previous implementation: [buffered temporal output](3d-solver-feasibility.md#buffered-temporal-output-and-resource-limits--2026-09-24) groups canonical JSON tokens into at most 64 KiB writes while retaining strict byte caps, actual persisted-byte/hash accounting and final-file preservation. The declared **326,262,706-byte** case now completes its supervised worker phase in **81.291 seconds** and its separate audit in **56.452 seconds**, within unchanged 3 GiB limits. Its baseline reached the 600-second hard deadline with no final snapshot; that failure remains in the [baseline ledger](3d-temporal-output-resource-results.json). All four buffered ordinary sizes match independently predicted complete bytes and pass fresh audits. Five fault controls in each version preserve stopped, failed, incomplete and complete-prefix meanings. The [buffered ledger](3d-temporal-buffered-output-results.json) also binds **198 passing temporal/process tests in 29.344 seconds**, including fourteen new independent methods and all 293 frozen inputs. The initial read-only Warp-cache test failure is retained; the successful retry changes only the bounded cache location. This is a synthetic output/resource witness, not new motion or accuracy evidence. Next adapt separate response/source/path audits before a newly declared cached accuracy study.

Independent evidence review verifies all eighteen cases and forty-nine phase containers, source/artifact identities, failed-process distinctions and test results. For that buffered checkpoint, local declarations, copied inputs, raw outputs and review scripts remain in ignored `.planning/solver/` in `/private/tmp/sew-computer-3d-temporal-output-budget`; its committed ledgers bind them but do not vendor them. `python3 .planning/solver/package-resource-results-v1.py --check` and the matching `package-resource-results-v2.py --check` reproduce the committed resource ledgers. Re-execution requires a new prospective declaration and fresh output names; preserve the failed baseline and prior test/reviewer invocations.

Previous implementation: [retained temporal prefix admission](3d-solver-feasibility.md#retained-temporal-prefix-admission--2026-09-24) independently binds raw snapshot/run/context identities, reconstructs the coarse/fine scheduler and reservation costs, and reproduces whole-pair state and conditional indicator/energy accounting. **1,545 full numerical/supervision tests pass in 883.635 seconds**, including 41 new methods. The overlapping **108-test** focused run passes in 14.521 seconds, including 25 independently authored serialized attacks and sixteen primary methods. All 239 frozen inputs remain unchanged. The [ledger](3d-temporal-admission-results.json) preserves actual exits and independent review. Invalid diagnostic state cannot become committed progress; a complete prefix does not establish a successful process. This earlier full-suite result is historical, not a new full numerical or application run for the buffering change.

Previous implementation: [bounded temporal process evidence](3d-solver-feasibility.md#bounded-temporal-process-evidence--2026-09-24) now connects an unused retained-execution handle to Linux signals, private streaming raw export and actual parent-observed wait/cleanup results. Numerical completeness stays separate from stopped or failed process status. **1,494 full numerical/supervision tests pass in 921.705 seconds**, including fifty new methods, with all 235 frozen inputs unchanged. Ten further independently drafted fault methods pass separately in 3.203 seconds, binding 236 inputs with unchanged producer bytes; the overlapping 117-test focused run also passes. The [ledger](3d-temporal-process-results.json) preserves counts, actual exits and independent review. Snapshots are byte-observed evidence awaiting separate structural/numerical audit, and hard kills may leave none. Close separate raw admission/audit and measured large-output budget prerequisites before declaring the next cached adaptive study. The existing captured supervisor, protected entrypoints, application and production are unchanged.

Current study: [exact-contact temporal verification](3d-solver-feasibility.md#exact-contact-temporal-study-and-retained-reference-audit--2026-09-24) retains three CPU-limit failures: A/B return no accepted histories, and uniform4096 leaves 4,096 states plus 2,543 original audits without a successful terminal receipt. Uniform2048 completes all 64 ms and audits 2,048 states. All six original comparisons are unavailable; temporal accuracy remains unresolved. Static energy reduction finds 60 A and 185 B definite local allocation exceedances on the coarse reference, concentrated in the transient; all 1,024 released intervals meet both allocations. These are stored-path tests, not reconstructed adaptive decisions.

A separately declared supplemental audit completes all **4,096** saved fine-reference intervals and reproduces the original **2,543** audit files byte-for-byte. The new two-reference comparison fails both targets: **7.304983 µm at 64 ms** and **11.874280 mm/s at 5.15625 ms**, against 1 µm and 0.25 mm/s. It does not make the original failed process successful or recover adaptive histories.

The [ledger](3d-exact-contact-temporal-results.json) preserves the frozen study and actual process exits. All 249 original inputs remain unchanged. The prior **1,406 full tests in 841.366 seconds** remain the numerical baseline for this evidence increment; no application or deployment change is claimed. The raw controller-prefix increment above now passes its declared regression scope; large-output budgeting and separate adaptive-response/source/path audit adaptation still precede the cached adaptive study. The entries below preserve earlier checkpoints and their then-current next steps.

Previous implementation: [retained temporal execution](3d-solver-feasibility.md#retained-temporal-execution--2026-09-24) adds an explicit single-use stop handle, a persistent request latch and one authoritative in-memory committed prefix. Raw snapshots retain admitted controls and truthful reservation/dispatch/return observations before optional report enrichment. **1,444 full numerical/supervision tests pass in 847.777 seconds**, including 26 new methods; all 232 frozen inputs and snapshots match. The overlapping 83-test temporal suite and seven exact previous/current controller comparisons pass. The [ledger](3d-temporal-stop-results.json) retains actual exits and independent review. Default behavior remains unchanged in the bounded comparison; immediate native cancellation, durable recovery and persistent resume are not established.

The frozen study and supplemental audit above are complete as evidence, with temporal accuracy still unresolved. The bounded process increment at the top supersedes this retained-handle checkpoint’s next-step note. Numerical completion, a requested stop and actual process outcome remain separate. Full temporal/spatial, construction, material/body and application gates remain open. The entries below preserve preceding checkpoints and their then-current next steps.

The preceding documentation-only integration preserved the numerical source tree verified by the **1,444-test** stop-support checkpoint. Original study, cache and stop executions retain their separate source snapshots and artifact inventories in their original isolated checkouts. Their historical hashes describe those frozen executions, not every later checkout. That earlier integration reran no numerical suite and did not combine audit counts with test counts.

Previous implementation: [exact contact geometry reuse](3d-solver-feasibility.md#exact-contact-geometry-reuse--2026-09-24) preserves the existing rational arithmetic and per-call admission while retaining at most 256 geometry entries. **1,418 full numerical/supervision tests pass in 856.326 seconds**, including twelve new methods; all 228 frozen inputs match. Independent review confirms exact arithmetic/work equivalence in its bounded corpus and identical complete work/native records for the six-pair static benchmark. Its 72 timed calls use approximately **44.75% less CPU time**; full-solver speed and trajectory equivalence are unestablished. The [ledger](3d-contact-geometry-cache-results.json) preserves source/result identities, actual exits, review repairs and provenance limits.

Next finish the separately frozen uncached temporal study before integrating this cache into that checkout, then declare prospective cached controls. No temporal, spatial, construction, physical or application acceptance follows from this optimization. The entries below preserve earlier checkpoints and their then-current next steps.

Previous implementation: [exact finite-feature construction](3d-solver-feasibility.md#exact-finite-feature-contact-construction--2026-09-24) adds an explicit optional contact model that classifies candidates before activation, preserves source-contribution multiplicity and uses the same forced feature for native responses and bounded work. **1,406 full pinned Linux numerical/supervision tests pass in 841.366 seconds**, including 22 new methods and actual guarded synthetic motion; all 227 frozen inputs remain unchanged. Static evaluation admits **all 2,049 retained endpoints**, covering **43,672 supplied candidates and 26,418 active contributions**; this closes the previously observed sixteen endpoint feature disagreements under the changed model. Independent complete conversion-identity review passes on every state, preserving duplicates in 1,034 states. The [ledger](3d-exact-contact-results.json) binds the results and source identities. The first focused run's six diagnostic-cache identity failures are retained and repaired. No new research trajectory or temporal-accuracy acceptance is claimed.

Next explicitly declare the changed model in prospective temporal verification. B's energy/depth feasibility, original preload, released tail, reference self-agreement and all construction/material/full-shirt/application gates remain open. The entries below preserve earlier checkpoints and their then-current next steps.

Current diagnostic: [native near-parallel feature review](3d-solver-feasibility.md#native-near-parallel-feature-diagnosis--2026-09-24) explains all **16** retained rejections and reproduces the selected formulas in the compiled library. Exact/static witnesses expose selector energy jumps and a native activation gate that omits a supplied geometrically active candidate, including with positive surface-area weighting. A captured-branch scalar alone does not repair global force/work consistency. Keep the current rejection and implement an explicitly versioned exact finite-feature policy before activation and consistently through inventory, energy, derivatives, work and model identity. The [diagnostic ledger](3d-contact-feature-diagnosis-results.json) retains successful checks, failed review/runtime attempts and limits. This increment changes no numerical source or trajectory; all 225 inputs from the preceding 1,384-test baseline remain unchanged.

Previous implementation: [bounded contact-work integration](3d-solver-feasibility.md#bounded-contact-work-integration--2026-09-24) connects an explicit numerical policy to conditional line search, complete mechanical/parameter accounting and temporal energy radii. **1,384 full pinned Linux numerical/supervision tests pass in 830.473 seconds**. The original failed 3.90625 µs trial now converges in **3 evaluations at 7.04e-13 N**, with its original 1e-8 N criterion, geometry and controls. Independent saved-result verification passes exact/high-precision contact work, control/state identity, conditional force, cable work and complete physical-path checks. Four adversarial publication/control repairs and both first-full-run Mock-fixture failures remain retained.

The [integration ledger](3d-contact-work-integration-results.json) also records complete static coverage of the retained 2,048-step reference: **2,033 endpoints admit and 16 reject** exact native-feature agreement. Do not drop their small positive mollified terms or silently choose another feature. Resolve this coverage gap before promoting the policy to a full same-input accuracy study; B's energy/depth feasibility, preloaded controls, released-tail and reference self-agreement remain required. A repaired local solve is not full temporal resolution. Construction, material/body, full-shirt and application gates remain unfinished.

The primitive and earlier entries below retain preceding checkpoints and their then-current next steps.

Previous increment: the [bounded contact-work primitive](3d-solver-feasibility.md#bounded-contact-work-primitive--2026-09-24) independently encloses all **54 saved endpoint pairs / 972 native contributions** using exact input geometry, captured native constants and bounded logarithms. **81 focused pinned Linux tests pass in 6.998 seconds**, including 30 new methods; complete native recapture also passes. The [ledger](3d-contact-work-results.json) retains the exact scope and adversarial failures. A near-parallel native feature mismatch explicitly rejects, so the primitive does not cover every native-admitted state. Ordinary record reinitialization is repaired; positive native cached weights remain trusted model inputs.

The new arithmetic and native capture are standalone. Existing solver/line-search/energy/temporal code and both protected entrypoints remain unchanged; no new research trajectory is executed. Next propagate the new work radius through an explicit numerical policy into conditional Armijo and temporal energy accounting, then replay the failed trial and repeat a newly declared same-input study. Preserve force criteria, complete endpoint inventories and geometric path guards. The prior **1,331-test** full numerical suite is a historical baseline, not rerun or combined with the focused count. Temporal/spatial accuracy, construction operations, material/body evidence, full-shirt acceptance and assembled-view integration remain unfinished.

Previous increment: the [same-input temporal study](3d-solver-feasibility.md#same-input-temporal-study-and-search-failure--2026-09-24) executes both declared adaptive policies and 1024/2048 uniform references. **3,290 new accepted numerical states** pass retained-prefix audits, including **71,226 contact leaves and 13,160 triangle leaves**. Both adaptive histories stop during early contact: A at **2.1328125 ms** after a nonlinear failure, B at **1.91796875 ms** when the fine energy allocation still fails at maximum depth. The two full 64 ms references disagree by **2.59534 µm** in position and **9.20945 mm/s** in native velocity at shared stored endpoints, exceeding the declared 1 µm/0.25 mm/s goals. Temporal accuracy remains unresolved. The [ledger](3d-temporal-accuracy-study-results.json) and [figure](3d-temporal-accuracy-study.png) retain the failed criteria and comparisons.

A passive replay reproduces A's failed 3.90625 µs trial. Contact-gradient quadrature changes sixteen recorded descent predicates relative to native endpoint energy subtraction; this is a diagnostic of arithmetic sensitivity, not a certified replacement or an accepted alternate trajectory. Next independently validate stable contact-work differences against the pinned collision/barrier law, repair the small-update search arithmetic without weakening force or Armijo criteria, and repeat a newly declared same-input study. B's depth/energy feasibility remains a separate issue. Independent review reconstructs every adaptive assessment and checks the comparison/trace evidence. All 226 frozen study inputs remain unchanged; the prior **1,331-test / 845.829-second** regression remains the numerical-source baseline and is not rerun for this evidence-only increment. Spatial, construction, material/body, full-shirt and application gates remain open.

The controller and earlier entries below retain preceding checkpoints and their then-current next steps.

Previous increment: [conditional temporal control](3d-solver-feasibility.md#conditional-temporal-controller--2026-09-24) now compares a full interval with two guarded half intervals, uses separate maximum-vertex position/velocity indicators and checks each fine interval against an absolute conditional numerical-energy budget. Only the fine path is committed. Original schedules, model identities, exact physical durations, fixed precision and complete trial costs remain explicit. Retained states permit checking both fine physical segments. Existing journals and per-step callbacks are rejected by this synchronous in-memory policy; captured source and durable persistence are not added.

**1,331 full pinned Linux numerical/supervision tests pass in 845.829 seconds**, including 38 new methods. The overlapping 199-test focused run passes in 29.351 seconds. Final independent review finds no remaining demonstrated blocker in its bounded scope. The [controller ledger](3d-temporal-controller-results.json) records scope and evidence. Next freeze and execute a same-input temporal-resolution study on the contact transient and released tail, with feasible declared budgets. Local indicators and energy allocations do not establish phase/global accuracy or temporal convergence. Spatial, construction, full-shirt and application gates remain open.

The interval-trial and earlier entries below retain preceding checkpoints and their then-current next steps.

Previous increment: [contact timescales and private interval trials](3d-solver-feasibility.md#contact-timescales-and-private-interval-trials--2026-09-24) analyzes 86 retained tight-run states and separates guarded interval evaluation from accepted-state publication. At 2 ms, local contact tangent periods are approximately **1.32 and 1.16 ms** on the 128/512 grids; the corresponding timestep products are **2.372 and 0.676**. Local frequency bands carry most saved kinetic energy there, supporting targeted investigation of this transient without establishing causality or temporal convergence. The [ledger](3d-contact-timescale-results.json) and [figure](3d-contact-timescale.png) retain the numerical scope. The static diagnosis executes no new research trajectory; regression tests still exercise solver motion fixtures.

The private evaluator preserves the original problem/schedule and all existing numerical, work and publication checks. **1,293 full pinned Linux numerical/supervision tests pass in 838.696 seconds**; the 132 focused tests overlap that count. Independent review confirms the moved body and surrounding orchestration structurally unchanged, plus 32 old/current result-and-event comparisons. A temporal controller is **not yet implemented**. Exact oscillator and inverted-quadratic witnesses show why neither coarse/fine agreement alone nor numerical-energy accounting alone certifies state accuracy. Next implement nonpublishing coarse/fine trials with separate state indicators, an explicitly conditional numerical-energy budget, complete resource accounting and fine-path commitment, then independent refinement verification. Capture/replay entrypoints remain unchanged; their pending authorization is not bypassed.

The following stopping-criterion and earlier entries retain preceding checkpoints and their then-current next steps.

Previous increment: [explicit force stopping and matched temporal sensitivity](3d-solver-feasibility.md#explicit-force-stopping-and-matched-temporal-sensitivity--2026-09-24) adds a tightening-only direct-solver criterion with guarded adaptive/final publication. The 1e-6 N default is preserved. Matched 1e-8 N runs at 128/512 intervals complete **640 new states, zero rejects and 320 fully released intervals**. All **13,962 contact leaves and 2,560 triangle leaves** pass the adapted saved-evidence audit. The [ledger](3d-tight-stationarity-results.json) and [figure](3d-tight-stationarity.png) retain the declared physical inputs, criterion, actual partitions, source/artifact identities and conditional balance reductions.

The tighter criterion reduces final accumulated saved-force velocity defects from **1.783912 to 0.00203895 mm/s** at 128 intervals and **0.409526 to 0.00500687 mm/s** at 512. These hold the saved numerical forces fixed; they are not bounds on another trajectory. The dominant common-0.5-ms cross-grid native-velocity discrepancy remains **5.953707 versus 5.954307 mm/s** under default and tight criteria. Temporal convergence and physical settling remain unestablished. Next resolve early contact/control dynamics with a declared temporal-accuracy target and explicit dissipative semantics, then spatial sensitivity and construction-specific joints.

Verification passes **1,293 full pinned Linux numerical/supervision tests in 813.512 seconds**, including 21 new cases; focused macOS/Linux counts overlap. Independent source review passes 62 checks, and separate saved-state reconstruction verifies all 3,088 sample metrics and 24 global maxima across eight comparison grids. The six-run primary balance reduction uses 1,600 states (640 new, 960 reused); independent comparison covers the four matched runs with 1,280 states. The full saved-contact audit remains a primary-agent adaptation of independently authored helpers. Refined capture/replay entrypoints remain unchanged; strict requests reject their legacy journal schema. Refined entrypoint changes and remote publication remain pending specific authorization. Construction, calibrated materials/body evidence and full-shirt application acceptance remain unfinished.

The following finer-grid and earlier entries retain preceding checkpoints and their then-current next steps.

Previous increment: the [finer separated-pose comparison](3d-solver-feasibility.md#finer-separated-pose-temporal-comparison--2026-09-24) adds 256/512 intervals with **768 new states, zero rejects and 384 fully released intervals**, reusing 192 states from 64/128. Complete physical inputs, runtime, precision, force criterion and resource limits match; only subdivisions change. All **16,713 contact leaves and 3,072 triangle leaves** pass saved-evidence checks. The [finer-grid ledger](3d-cable-separated-finer-results.json) and [figure](3d-cable-separated-finer.png) retain the input hash, actual partitions, source/artifact identities and full comparison scope.

Temporal convergence remains unestablished. Successive fixed-2-ms position discrepancies are **3.84669, 7.19700 and 5.24359 µm**, while native-velocity discrepancies grow **1.27284 → 2.32116 → 3.81542 mm/s**. Common 2 ms displacement secants improve, which does not establish native-velocity convergence. A denser 1 ms comparison reveals an early **5.35569 µm** 64/128 position peak missed previously; tail dominance applies only to the former 2 ms observation grid. Final maximum speeds rise to **1.674184 and 1.880015 mm/s** on the new grids. Next diagnose contact-engagement and release-phase sensitivity using these saved trajectories before extending time grids, then establish spatial sensitivity and construction-specific joints.

The primary agent adapted earlier independently authored exact/high-precision audit helpers; no new independent second-author review is claimed. All seventy-nine numerical/entrypoint/fixture files remain identical to `792cdb6`, and the prior **1,272-test / 801.821-second** suite remains the unchanged baseline. Two plotting-runtime failures are retained; the existing plotting environment produced the figure without changing any numerical input. Refined capture/main-replay and remote publication remain pending specific authorization. Garment construction, calibrated material/body evidence and full-shirt application acceptance remain unfinished.

The following preload and earlier entries retain preceding checkpoints and their then-current next steps.

Previous increment: the [initial contact preload comparison](3d-solver-feasibility.md#initial-contact-preload-and-cable-interaction--2026-09-24) completes **576 new states with zero rejects**, plus 192 reused original scheduled states. A proper upper-layer initial translation by 1/8192 m preserves rest geometry and removes initial contact E/g/H. With cables inactive, the original pose retains strongly timestep-dependent motion while the separated pose becomes tolerance-limited after one small correction. With scheduled cables, 64-versus-128 position disagreement falls from **200.515 to 3.84669 µm** for the changed initial pose. The [preload ledger](3d-cable-contact-preload-results.json) and [figure](3d-cable-contact-preload.png) retain all eight arms and their limits.

The remaining discrepancy is now dominated by the released tail; scheduled final speeds remain approximately **1.000 and 1.300 mm/s**. Initial separation also changes cable extension, so this is not a pure preload intervention or convergence proof. Next refine the same separated scheduled physical inputs on additional declared grids and examine both whole-window and release-phase errors. Keep the original preloaded case as a numerical regression. Source-specific joints, binding/cuff construction, temporal/spatial stability, material/body evidence and full-shirt application acceptance remain unfinished.

The primary agent adapted and ran existing independently authored exact/high-precision audit helpers after live reviewers reached account usage limits. New checks cover **5,979 contact leaves, 2,304 triangle leaves and 288 released intervals**; no new independent second-author review is claimed. All seventy-nine numerical/entrypoint/fixture files remain identical to `b9f0a95`, and the prior **1,272-test / 801.821-second** suite remains the unchanged baseline. Review-runtime, membrane bit-equality and ledger-counting failures are retained without changing numerical sources or acceptance thresholds.

The following loaded-pair and earlier entries retain preceding checkpoints and their then-current next steps.

Previous increment: [two loaded cable cells and a fully released tail](3d-solver-feasibility.md#loaded-cable-pair-and-released-tail-timestep-comparison--2026-09-24) now execute with unchanged generic mechanics. Three matched 64 ms runs at 32/64/128 uniform intervals complete **224 accepted states with zero rejects**, including **112 fully released intervals**. Independent response/work/path checks pass; both cells are loaded through the shared 12–16 ms interval, and cable response/work is exactly zero on the 32–64 ms tail. Every saved state moves. The [loaded-pair ledger](3d-cable-loaded-tail-results.json) and [figure](3d-cable-loaded-tail.png) preserve the numerical results and their limits.

Temporal convergence remains unresolved. Peak position disagreement on the common 2 ms grid grows from **0.0960 to 0.2005 mm** as the timestep halves; native velocity and common-window secant discrepancies also grow. Net parameter work changes sign. Tail-only agreement improves, but final speeds of approximately **0.666, 1.000 and 1.300 mm/s** do not establish settling. Initial contact preload and changing activation are confounded before the first target ramp. The prospective next diagnostic crosses original/separated initial poses with scheduled/zero activation at 64/128 intervals, retaining all other settings; it has not executed. Separation is a declared input change, not an equivalent trajectory or proven fix.

All seventy-nine numerical/entrypoint/fixture files remain byte-identical to `624a45c`. Its **1,272 full pinned Linux numerical/supervision tests passed in 801.821 seconds**; this evidence-only increment does not rerun that suite or count saved-state audits as new unit tests. Construction-specific joint/layer semantics, joint spatial/temporal seam verification, binding wrap/finish/apex, cuff turning, material/damping/friction/body evidence and full-shirt application acceptance remain unfinished. Refined capture/main-replay and remote push still await specific authorization; do not bypass those boundaries through the generic API.

The following varying-control and parameter entries retain preceding checkpoints and their then-current next steps.

Previous increment: [guarded varying cable controls](3d-solver-feasibility.md#guarded-varying-cable-controls--2026-09-24) execute changing targets/activation in the generic direct solver, adaptive retries and complete work accounting. Each nonlinear trial uses fixed effective controls; retries sample original fractions and require an exact initial parameter match. Fresh endpoint/parameter/motion evaluations, final mutation checks, detached journals and required public scalar fields protect publication. The [varying-control ledger](3d-cable-varying-results.json) binds the evidence. **1,272 full pinned Linux numerical/supervision tests pass in 801.821 seconds**, including **55 new cases**. The fifty-five and two changed legacy journal fixtures also pass together on macOS and Linux; these counts overlap.

The final synthetic comparison is retained at `.planning/solver/cable-varying-contact-motion-sep24-v2/`. Both disabled/scheduled cases complete sixteen uniform 2 ms steps. Scheduled final center gaps are 2.11070–2.11365 mm and maximum speed is 0.826066 mm/s; all states move, so no settling claim follows. The center cell engages, tightens, holds and releases. The independently scheduled outer cell stays slack; it is not a second loaded seam. Both activation values reach zero only at the final endpoint. The first motion version predates the public-field validation repair and remains retained separately, along with the deliberately stopped incomplete first full regression.

Next use unchanged generic mechanics to test a genuinely loaded second cell and an explicit fully released interval, then temporal/spatial refinement with identical physical controls. Preserve the discrete work convention and error propagation rather than interpreting release removal as heat. Construction-specific joint/layer semantics and joint spatial/temporal seam verification remain next source gates. The candidate cable permits collapse, wrong sides and tangential/rotational freedom; it does not establish binding wrap/finish/apex, cuff turning or a sewn attachment. Material/damping/friction/body evidence and full-shirt application acceptance remain unfinished. Refined capture/main-replay and remote push remain pending specific authorization; do not bypass those boundaries via the generic API.

The entries below retain the preceding checkpoints and their then-current next steps.

Current parameter prerequisite: [cable target and activation parameters](3d-solver-feasibility.md#cable-target-and-activation-parameters--2026-09-24) adds two standalone modules: an immutable supplied-cell recipe with bounded target-first parameter work, and an immutable schedule with exact original-fraction interpolation and finite-grid coefficient admission. Total work retains tiny differences and cross-cell cancellation; four optional component tolerances remain separate from the total tolerance. Reinitialization and ndarray metadata attacks are repaired and retained. These modules execute no motion. The [parameter ledger](3d-cable-parameter-work-results.json) contains the implementation evidence and failed development history.

Next integrate changing controls into the generic guarded solver, adaptive retries and complete work publication. Preserve the exact beta law, original fractions, immutable per-trial potentials and target-first work at the previous positions. Then compute motion work under the new controls. The standalone schedule permits an initial parameter mismatch but accounts for no jump; integration must require an exact initial match or explicitly account for that jump. Do not infer continuous actuator work from this discrete convention. Keep source-specific joint construction and joint spatial/temporal seam verification separate, and do not bypass pending refined capture/main-replay authorization through the direct API.

The retained prospective integration design in the parameter ledger chooses an exact initial numerical-parameter match and rejects an implicit initial jump. It requires fresh numerical parameter/motion-work comparisons after the energy helper, then final state/control/report checks before publication. A separate five-case exact two-particle balance oracle and independent byte-identical reproduction cover engagement, tightening, release and both slack crossings. These are design fixtures for the next implementation, with no solver execution or additional unittest count; their backward-Euler attenuation is not material damping.

The exact polynomial tests, four-boundary examples, high-precision numerical comparisons and selected independent analytic brackets now exercise the implemented work helper. Finite-grid schedule checks cover round-to-even interpolation, signed zero, underflow and bounded preflight through a 2^40 denominator. The twenty-four additional work probes are distinct from test-method counts, and general quadrature is not a second rigorous proof. Construction, temporal/spatial convergence, material/body validation and full-shirt application acceptance remain open.

Previous mechanical increment: [guarded continuous cable integration](3d-solver-feasibility.md#guarded-continuous-cable-integration--2026-09-24) adds the supplied-cell candidate to the generic solver, adaptive retries and energy accounting. Cable uncertainty and final addition rounding enter stationarity and interval Armijo decisions, conditional on existing non-cable numerical terms. Immutable controls, strict raw admission, fresh endpoint/work comparisons and final report snapshots protect accepted-step publication. Fifty-four new focused tests pass on macOS and pinned Linux; full discovery passes 1,179 tests. The matched synthetic control completes thirty-two steps with independent contact/triangle checks. This is generic research mechanics, with no source or garment admission.

At that fixed-control checkpoint, separately declared cable schedules and parameter work remained next. The standalone modules above now provide those prerequisites; execution through guarded retries and work publication is still required. Then address source-specific joint controls and joint spatial/temporal seam verification. Current controls and precision are fixed for the entire run; unresolved precision rejects. The law permits collapse and wrong sides, and independently rounded anchors can introduce small net-force defects. Actual construction, temporal/spatial convergence, material/body validation and full-shirt acceptance remain open. Do not use the generic API to bypass the pending refined capture/main-replay authorization.

The [integration ledger](3d-cable-integration-results.json) also retains the next-step design and a separate standard-library exact arithmetic oracle: nineteen explicit fixtures and 243 rational cases, including cancellation, release and subnormal work. A four-irrational-root witness is boundary-only. These are future-design checks, not implemented varying controls or additional numerical-suite tests. Preserve the exact beta law and target-first work convention; release must retain its intermediate target energy, and work differences need not be representable as positive binary64 values when bounded correctly.

The synthetic fixed-control pair is retained at `.planning/solver/cable-contact-motion-sep24-v2/`. Both cases complete sixteen uniform 2 ms steps. The enabled cable limits final center gaps to about 2.092 mm versus 3.53 mm disabled; it does not close below the approximately 2 mm initial gap. Enabled states twelve through sixteen reuse unchanged positions under the force tolerance. No physical settling or damping claim follows. The [figure](3d-cable-integration.png), complete path proofs, independent response comparisons and failed-oracle history are bound in the ledger.

Previous mechanical increment: the [continuous tension-only candidate](3d-solver-feasibility.md#continuous-tension-only-sewing-candidate--2026-09-24) supplies bounded energy, gradient, generalized curvature and fixed-parameter work over declared material cells. Thirty-six new focused tests passed on macOS and pinned Linux; the standalone checkpoint passed 1,125 full tests. Independent static source checks passed within the published bounds. The initial near-zero energy is uncertainty-dominated and must be reported with its bound. The [primitive ledger](3d-continuous-cable-sewing-results.json) retains that earlier evidence; it is not a calibrated seam.

Previous mechanical increment: [continuous normal-offset sewing](3d-solver-feasibility.md#continuous-normal-offset-sewing-penalty--2026-09-24) supplies a standalone integrated penalty with frame reactions, full curvature, stable fixed-parameter work and explicit normal enclosures. Twenty independent tests include sample-invisible deformation, measure-preserving subdivision, cancellation and geometric precision. The one-sided director law can conflict across a crease and is not accepted as the garment joint. The subsequent symmetric-distance review also retains its constant-direction zero-set limitation. Source choices, work accounting, guarded integration and construction validation remain open.

Current spatial prerequisite: [continuous material-seam geometry and fixed-state bounds](3d-solver-feasibility.md#continuous-material-seam-geometry-and-fixed-state-bounds--2026-09-24) now cover all eight selectors and preserve all forty rows. The exact traversal retains tiny positive cells, full parent tiling and original/refined operator discrepancies. A constructed source state preserves sampled sewing vectors before rounding while its interior gap deviates from the approximately 1 mm target by up to 0.488 mm; independent exact extrema agree with the new diagnostic. The initial held curve passes the declared tolerance and the constructed one fails. This supplies complete material-path geometry at one state, without installing a continuum seam force law or proving temporal/contact paths. Next define and validate the physical joint and construction-specific controls; stable motion and full-shirt acceptance remain unresolved.

Current frame prerequisite: [source-bound candidate correspondence](3d-solver-feasibility.md#source-bound-sewing-frame-correspondence--2026-09-24) preserves all forty original/numerical rows and enumerates forty-two oriented frames with exact parent lineage. Row indices 27 and 37 (zero-based) need explicit candidate choices; all forty mechanical signs and all five textile-side declarations remain unresolved. An exact constructed fold keeps the ambiguous anchors fixed while their candidate normals become orthogonal. Tiny allowance coefficients also prevent an all-body attachment request under this model. The descriptor is static, preserves the existing scalar-distance reference and grants no refined-source execution or garment acceptance. Finish construction-specific frame/model decisions separately from the pending captured integration.

Previous numerical continuation: [finer coupled-control motion](3d-solver-feasibility.md#finer-mixed-control-motion-and-numerical-attenuation--2026-09-24) completes and independently audits all 2,017 states across 32–1,024 requested intervals. The finest uniform controls expose relative layer opening and much more motion than the coarse results. No material damping is installed; a saved-state energy diagnostic separates numerical attenuation from physical settling claims. Initial/engagement transients and temporal/spatial sensitivity remain unresolved. The eight-vertex fixture has one held distance sample, not a spatial seam or rotational joint. Actual source construction, material/body validation and full-shirt integration are still required; the refined-source capture/main-replay proposal remains unapplied pending specific authorization.

## Repository and scope

Preceding bounded-contact integration verification: **1,384 full numerical/supervision tests pass in 830.473 seconds** on the pinned Linux image, with all **225 frozen source/test/runtime inputs unchanged**. The full receipt is `.planning/solver/contact-work-integration-full-numerical-linux-execution-sep24-v2.json`. The first complete run's two Mock-fixture failures and independent repairs remain retained. The one-trial replay binds **239 inputs**, preserves the original physical state/control problem and completes with all identities unchanged. Both protected entrypoints and the application remain unchanged. See the [curated integration ledger](3d-contact-work-integration-results.json) for local artifacts and scope.

Preceding temporal-controller implementation baseline: **1,331 full numerical/supervision tests pass in 845.829 seconds**, with all 218 frozen source/test/runtime inputs unchanged during execution. Full log: `.planning/solver/temporal-controller-full-numerical-linux-sep24-v1.log`. Relative to `e296b0e`, two existing numerical modules change and one controller plus two test modules are added; the other 213 frozen inputs remain identical. Both protected capture/replay entrypoints remain identical. Independent review verifies exact indicator/energy arithmetic, bounded trial transactions, raw control sampling, interruption prefixes, repaired work-report admission and thirty-two default compatibility traces. At that implementation checkpoint, regression and review particle fixtures execute synthetic motion; no new research garment/cloth trajectory, application release, push or deployment is claimed. The subsequent same-input study at the top of this handoff executes new generic cloth trajectories and a separate failed-trial replay.

Previous interval-trial verification: **1,293 full numerical/supervision tests pass in 838.696 seconds** with all 215 frozen source/test/runtime inputs unchanged during execution. Full log: `.planning/solver/interval-trial-full-numerical-linux-sep24-v1.log`. Relative to `965eba9`, only `solver_adaptive_contact.py` changes among those inputs; all other 214 remain identical. The static timescale analysis ran before that extraction and its 77 solver-source identities match `965eba9`. Independent review separates structural/mock equivalence, exact analytic design witnesses and saved numerical reductions. This checkpoint remains local research, with no application release, push or deployment.

Previous force-criterion verification: **1,293 full numerical/supervision tests pass in 813.512 seconds** with all 215 frozen source/test/runtime inputs unchanged during execution. Full log: `.planning/solver/tight-stationarity-full-numerical-linux-sep24-v1.log`. Relative to `1a57c37`, three numerical modules change and a criterion helper plus 21-case test module are added; the other 76 files in the experiment manifest remain identical, including both capture/replay entrypoints. The ledger distinguishes primary-adapted saved-contact checks from independently authored source/input/balance/comparison reviews. This checkpoint remains local research, with no application release, push or deployment.

Previous preload verification: six new runs pass all 576 saved-state response/work/path checks, including 5,979 contact and 2,304 triangle leaves. Four static preflights retain exact initial-pose checks and measured force components. The two original scheduled runs contribute 192 reused states, with all 783 declared baseline artifact identities rechecked. New runs and reused controls share the pinned runtime. The committed ledger binds full local inventories and explicitly records primary-agent adaptation of prior independently authored audit helpers. This evidence-only checkpoint does not push or deploy.

Previous loaded-pair verification covers all **224 saved states**, **112 fully released intervals**, **4,793 exact contact leaves and 896 triangle leaves**. Independent response/work comparisons and common-time reductions pass; the whole-window temporal convergence gate remains open. A separate high-precision reduction checks 294 metric triples, 224 kinetic energies and 28 sustained-loading cell/time witnesses. The recorded 1,272-test suite is the preceding `624a45c` baseline; all seventy-nine files in the current numerical/entrypoint/fixture manifest remain identical. Full run artifacts and reviewer outputs remain ignored local research data, bound by the committed ledger. This increment changes only evidence/documentation and is not pushed or deployed.

Previous varying-control verification: **1,272 full pinned Linux numerical/supervision tests pass in 801.821 seconds**. The 55 new cases and two changed journal fixtures pass together in focused macOS and Linux runs; counts overlap. Full log: `.planning/solver/cable-varying-full-numerical-sep24-v2.log`. Independent motion review verifies all 32 intervals, 663 contact leaves and 128 triangle leaves. All 134 case input/state/audit/report/journal files are byte-identical before and after the public-field validation repair; timings and the energy-helper source hash differ. The final source manifest is unchanged after the full run. Primitive/kernel/parameter/schedule and capture/main-replay files remain byte-identical to `77d7e21`; global/adaptive/energy integration changes are deliberate. This is offline verification with no application release, push or deployment.

Previous parameter verification: **1,217 full pinned Linux numerical/supervision tests pass in 788.479 seconds**, including **38 new cases**. All thirty-eight also pass in focused macOS and Linux runs; these counts overlap. Full log: `.planning/solver/cable-parameter-work-full-numerical-sep24-v2.log`. The first full run was deliberately stopped with exit 137 after independent review found the schedule array-metadata defect; it is neither a completed run nor a unittest failure. Final numerical sources, schedule storage repairs and independent references are bound in the parameter ledger. At that checkpoint all prior mechanical, solver, adaptive, energy and capture/main-replay modules remained byte-identical to `6a0b554`. The current varying increment deliberately changes global/adaptive/energy integration; capture/main replay remain unchanged. This is offline verification only; no application release, push or deployment is included.

Independent final standard-library review passes **119 identity checks across 66 files**, faithful reduction of all twenty-four work cases, separate exact-cancellation/reference handling and byte-identical ledger regeneration. It confirms all ten checked mechanics/entrypoint files match `6a0b554`, all thirty-eight new test methods and the prospective five-case balance/reviewer reduction. Final log: `.planning/solver/review-cable-parameter-work-ledger-final-sep24-v2.log`. The review artifact `.planning/solver/review-cable-parameter-work-ledger-final-sep24-v2.json` has SHA-256 `094e0dd4a71a7567253d2711336cbae43ddaab4550c9cfb72280f852f760dc86`. This is an evidence audit, not another numerical test run.

Previous fixed-control verification passes **1,179 full pinned Linux numerical/supervision tests in 779.794 seconds**, including **54 new cases**. The new cases also pass in focused macOS and Linux runs; counts overlap. Full log: `.planning/solver/cable-integration-full-numerical-sep24-v2.log`. The first full run found two open-ended legacy journal mocks missing the explicit no-cable field; only those fixtures changed, with strict numerical admission intact. The corrected Linux journal/cable set passes 78 cases and the two changed fixtures pass natively. A broader native journal attempt retains two Linux-only supervision failures. The cable primitive/kernel and capture/main-replay entrypoints remain byte-identical to `e59f8c7`; global/adaptive/energy integration changes are deliberate. No application build, release, push or deployment is included.

Independent standard-library ledger review passes **360 identity checks across 212 files**, exact byte regeneration and all thirty-two saved-path reductions, including 774 contact leaves and 128 triangle intervals. Final log: `.planning/solver/review-cable-integration-ledger-final-sep24-v1.log`. The audit artifact `.planning/solver/review-cable-integration-ledger-sep24-v1.json` has SHA-256 `04ac3dd3f2c2134986c54b744fda846bed660477230b48188175ecc7e51b1133`. Future-design oracle counts stay separate from implemented mechanics and numerical-suite counts.

Previous standalone verification passed **1,125 full pinned Linux numerical/supervision tests in 766.085 seconds**, including **36 new cases**. All thirty-six also passed in focused macOS and Linux runs; these counts overlap the full discovery. The full log is `.planning/solver/continuous-cable-full-numerical-sep24-v1.log`. The [primitive ledger](3d-continuous-cable-sewing-results.json) retains model counterexamples, complete identities, source/error audits and the repaired publication failure. At that checkpoint all five global/adaptive/work/capture/replay execution files remained byte-identical to `ece4c8d`; the current increment deliberately changes the first three, while capture and main replay remain unchanged. This is offline numerical evidence with no application build, browser, release or deployment claim.

Independent standard-library ledger review checks **96 identity records across 63 unique files**, all seventy-six prior point-input alternatives, published response bounds and the retained reductions. In-memory regeneration is byte-identical. The final log is `.planning/solver/review-continuous-cable-ledger-sep24-v3.log`; earlier v1/v2 logs retain auditor-only flat-gradient and grouped-case-count assertion repairs. The audit output `.planning/solver/review-continuous-cable-ledger-sep24-v1.json` has SHA-256 `c2cfe097786dd9a51946df64031d2b519c0ce89bc75fdb62493f3766578c1caf`. These reporting repairs changed no mechanical code or numerical evidence.

Previous normal-penalty verification passes **1,089 full pinned Linux numerical/supervision tests in 755.048 seconds**, including **twenty new independent cases**. All twenty also pass in focused macOS and Linux runs; these counts overlap the full discovery. The full log is `.planning/solver/continuous-normal-full-numerical-sep24-v1.log`. The [mechanics ledger](3d-continuous-normal-sewing-results.json) records frozen source/test identities, focused/full logs, adversarial failures and repaired precision probes. This is offline numerical verification, with no application build, release or deployment claim.

Previous spatial verification: **1,069 full pinned Linux numerical/supervision tests pass in 762.033 seconds**, including **41 new cases**: twelve producer/evaluator, thirteen independent-auditor and sixteen exact-bound tests. All forty-one also pass in focused macOS and Linux runs; these counts overlap the full discovery. Native source fixtures are regenerated on macOS; this does not establish cross-runtime bit identity. The retained audit reproduces byte-identically with only the standard library and six copied auditor modules. Independent review recomputes all 356 cells across the two retained states. The full log is `.planning/solver/binding-spatial-seams-full-numerical-sep24-v1.log`. Complete identities and scope are retained in the [spatial ledger](3d-binding-spatial-seams-results.json).

Previous frame-correspondence verification: **1,028 full pinned Linux numerical/supervision tests pass in 650.506 seconds**, including **26 new cases** (thirteen producer and thirteen independent-auditor tests). All twenty-six also pass on macOS and in focused Linux runs; these counts overlap the full discovery. The retained unresolved descriptor reproduces byte-identically in an isolated standard-library-only audit. Independent review checks all forty-two candidates and rejects twenty-eight malformed requests/descriptors. Complete identities, logs and the corrected raw-file/canonical-JSON hash comparison are in the [frame ledger](3d-binding-frames-results.json); the full log is `.planning/solver/binding-frames-full-numerical-sep24-v1.log`. These are offline static and numerical checks, with no application release or deployment.

Previous mixed-control verification: **1,002 full pinned Linux numerical/supervision tests pass in 596.167 seconds**, including **fifteen new cases** (six actual-mechanics and nine publication-validation tests). All fifteen also pass on macOS and Linux in focused runs; these counts overlap the full discovery. The [mixed-control ledger](3d-mixed-control-results.json) records complete logs, source and artifact identities, earlier failed development checks, and the independent saved-state review. The full log is `.planning/solver/mixed-control-full-numerical-sep23-v1.log`. This offline increment changes no application or deployment behavior.

Previous combined-declaration checkpoint: **987 full pinned Linux numerical/supervision tests pass in 601.119 seconds**, including **24 new cases** (twelve producer and twelve independent-auditor tests). All twenty-four also pass on macOS using fresh native fixtures; this does not certify cross-runtime source bit identity. The retained Linux declaration matches independent joint parameters at all **16,385 grid fractions across sixteen hinges and forty sewing rows**. Directed review checks all row identities/hashes and exact held-row witnesses, including tiny differences and four rejected forgeries. The audit reproduces byte-identically in an isolated standard-library-only process. Complete logs and artifact identities are in the [combined-control ledger](3d-binding-sewing-schedule-results.json); the full log is `.planning/solver/binding-sewing-schedule-full-numerical-sep23-v1.log`. No refined motion, source-admission change or application release is included.

Previous checkpoint verification: **963 full pinned Linux numerical/supervision tests pass in 487.079 seconds**, including **25 new cases** (twelve producer and thirteen independent-auditor tests). The new cases also pass on macOS using freshly generated native fixtures; this does not establish cross-runtime source bit identity. The retained Linux request matches independent parameter calculations at all **16,385 grid fractions across sixteen hinges**. Separate review passes 220 small-grid comparisons and retains fifteen activation-underflow rejections. The saved audit reproduces in a standard-library-only process. Log: `.planning/solver/binding-fold-schedule-full-numerical-sep23-v1.log`. These checks execute no refined motion and do not certify an application release.

The temporal declaration and complete finite-grid parameter comparison are retained under `.planning/solver/binding-fold-schedule-sep23-v1/`; see the [ledger](3d-binding-fold-schedule-results.json). These are parameter samples, not motion states. The synthetic integration below remains a separate experiment. A separate read-only seam audit is retained at `.planning/solver/binding-combined-sewing-audit-sep23-v1.json`: hold selector `bind_opening_left_left`, member one, rows zero through four, while rows five through thirty-nine remain pending. Preserve all forty historical targets and 1e-8 m/N compliance. Native initial arithmetic observes one ULP of sampled error on row zero; do not silently retarget it. The audit includes original and derived coefficient identities, source phase gates and runtime limits; it is retained as the antecedent input audit for the combined declaration above.

The preceding generic fold integration passed **938 pinned Linux numerical tests in 403.734 seconds**, including **28 new tests** (seven global integration, eleven adaptive control and ten energy accounting). Focused checks also pass on macOS and Linux. The retained full log is `.planning/solver/controlled-fold-motion-full-numerical-sep23-v1.log`; the motion ledger records source, state, review and test identities. This is offline generic numerical evidence, not refined garment execution or application release verification.

The generic API now accepts a separate immutable `controlled_fold_actuation` declaration, rebinds its ordered hinges to the actual native model, and requires explicit targets and activation on each direct step. Adaptive `fold_control_schedule` samples original fractions and validates inputs, reports and complete work before publication. All declared controlled hinges retain optimizer/physical path guards even after release. Source-profile admission and captured independent replay are separate remaining tasks. The earlier whole-strip gripper path is not an internal-fold recipe. Do not bypass the unapplied entry-point proposal with an alternate refined execution path.

The retained generic control is at `.planning/solver/controlled-fold-layered-motion-sep23-v1/`, generated by the sibling `.py` script through the direct API. It has eight synthetic vertices, four triangles, two free layers and no sewing rows. Its private callback artifacts are not a supervised crash-safe capture. See the [ledger](3d-controlled-fold-motion-results.json) and [saved-geometry figure](3d-controlled-fold-motion.png) for the exact scope. The previous standalone-primitive checkpoint passed 910 Linux tests in 394.011 seconds; subsequent counts below are historical.

The [source-bound refined placement](3d-solver-feasibility.md#source-bound-refined-placement--2026-09-23) now applies explicit poses to every unchanged numerical rest coordinate with exact binary-input arithmetic and one final rounding. The producer and standard-library auditor retain rotation defects, every edge/triangle witness, raw-lineage commutation and all ten cross-instance separating planes. A new uniform evaluation policy changes one historical binding vertex by at most 5.551115e-17 m; original source geometry and control targets are unchanged. No controls are installed or moved. The combined static declaration above supplies the temporal controls; next verify captured guarded motion after the pending entry-point authorization. The old whole-strip gripper targets are not automatically a valid internal-fold schedule.

The retained starting placement separately passes fixed-profile static contact admission: 0.1 mm activation/core, zero filtered pairs, zero contact energy/gradient, two nonadjacent intersection checks, 1,018 exact stationary candidate-separation witnesses with independent coverage and 398 stationary triangle witnesses. The minimum cross-instance plane gap is 1 mm. Raw v2 placement evidence is under `.planning/solver/binding-placement-sep23-v2/`; contact/runtime artifacts are under `.planning/solver/binding-placement-static-contact-sep23-v1/`. These checks execute zero time steps and do not certify motion from the original overlapping draft layout.

The preceding placement checkpoint passed **866 tests in 407.172 seconds** on pinned Linux, including all twenty-four new placement producer/audit cases; those twenty-four also passed on macOS. Log: `.planning/solver/binding-placement-full-numerical-sep23-v1.log`. This count and earlier counts below are historical checkpoints. Both proposed capture/main-replay entrypoints remain unchanged and their prepared patch still passes `git apply --check`; no application release check or deployment is included.

The [source-bound fold control](3d-solver-feasibility.md#source-bound-binding-fold-controls--2026-09-23) now derives both full-cut rails from directed source topology, binds all sixteen actuators to actual native hinge order and retains exact length/stiffness rounding witnesses. Its independent auditor imports only the standard library; a separate binder matches the declaration to the current native model and constructs the existing fold primitive. Relative-angle actuation applies balanced torques to free cloth, with no fixed-side assumption. A constructed two-fold pose requires transporting the finish axis after the attachment fold; the wrong flat-axis counterexample introduces 54.743 µm edge distortion. No dynamics or contact/path proof was executed for those constructed folded poses. The subsequent placement work supplies a separately checked starting state, not a folded trajectory; source-normal sewing frames and garment construction remain separate gates.

The [binding anchor remap](3d-solver-feasibility.md#binding-anchor-remapping--2026-09-23) now separates exact original coefficient lineage from explicitly rounded numerical weights. A new strict source profile rebuilds all global indices for 245 vertices and 398 triangles while retaining the immutable five-fabric base, all forty original rows and thirty-five unchanged derived local rows. Independent remap audit imports no implementation or pattern engine. Normal-offset sewing and its crease-side director frames remain unsupported in this profile; the separate fold mapping does not supply or override them.

The [gripper migration descriptor](3d-solver-feasibility.md#binding-gripper-migration--2026-09-23) now preserves original point controls while deriving their refined material anchors. The three existing grips map to canonical triangles 75, 55 and 86 with exact coefficient pullback; the allowance grip still has a separately recorded 1.788112e-18 m stored-coordinate shift. The independent standard-library audit recomposes each raw stage and reconstructs the complete recipe. No controls are installed and no refined placement or trajectory is produced. Source/control correspondence does not imply identical forces or motion after refinement.

Runner/main-replay integration is not applied: automatic approval review twice rejected edits to `scripts/spike-contact-continuation.py` and `scripts/replay-rest-filtered-continuation.py` for insufficient specific user authorization. The exact proposal is ignored at `.planning/solver/binding-refined-entrypoint-proposal.patch`, with `git apply --check` passing. Do not bypass the rejected action. After explicit authorization, apply/review the proposal, add entry-point regression tests and run a fresh captured refined control plus independent replay before claiming numerical integration. No new physics or physical acceptance follows from input correspondence.

The preceding static fold-control checkpoint passed **842 tests in 379.840 seconds** on pinned Linux, including all twenty-two new fold-control/audit cases, the preceding twenty-four gripper cases and forty seam-remap/source-builder/integration cases. Log: `.planning/solver/binding-fold-full-numerical-sep23-v1.log`. All twenty-two new tests also passed on macOS. Static geometry, source/control identities and independent rounding checks remain distinct from contact or dynamics proof. The two proposed entry-point edits remain unapplied; earlier counts below are historical.

The [extended binding hold](3d-solver-feasibility.md#extended-binding-hold--2026-09-23) completes and replays both 512-step controls, with the initial 192 transitions exactly matching the repeated baseline. Final maximum speed remains about 0.478 mm/s; passive-tail angle range increases to 0.05626°. The [ledger](3d-binding-extended-hold-results.json) and [figure](3d-binding-extended-hold.png) retain this unresolved motion. No settled pose or binding operation is accepted. Baselines were repeated from the identical complete captured numerical snapshot because capture includes newly added unused helpers.

The [binding refinement descriptor](3d-solver-feasibility.md#binding-crease-preprocessing--2026-09-23) adds attachment/finish crease chains through the complete original cut domain, yielding 29 strip vertices and 44 triangles with rationally composed original-parent lineage. It is deliberately non-executable. Seam, gripper and fold correspondence now have separate witnesses; normal-offset sewing frames and source capture/replay remain pending. Preserve the immutable original five-fabric unit and all forty source rows. Flat point containment alone cannot establish coefficient preservation under deformation; do not normalize, clamp or change the source profile to bypass validation.

This checkpoint's final pinned Linux numerical/supervision suite passes **756 tests in 347.358 seconds**, including all hold, refinement and repaired rigid-velocity cases. Log: `.planning/solver/binding-extended-hold-full-numerical-sep23-v2.log`. Earlier counts below describe historical checkpoints, not additional current tests. Nothing is deployed.

The [fourfold-duration comparison](3d-solver-feasibility.md#fixed-timestep-binding-time-control--2026-09-23) improves tracking at the unchanged 2 ms timestep: the 3° command peaks at 3.165° and finishes at 0.477 mm/s maximum speed. Both slower controls complete and replay 256 steps without retries. Relative angle still changes 0.03148° over the 64 ms passive tail. The [ledger](3d-binding-fourfold-time-results.json) and [figure](3d-binding-fourfold-time.png) preserve the original control alongside the slower outcome; neither is a settled-pose or construction acceptance result.

The [first five-fabric binding motion](3d-solver-feasibility.md#first-source-binding-motion--2026-09-23) now has matched zero/3° controls, complete independent replay and source-frame/surface observations. Both complete 64 steps without rejection. The driven run overshoots to 4.485° and ends with 0.01787 m/s maximum speed, so tracking and settling remain unresolved. Five sampled attachments stay held; wrapping, stitch-down, apex securing and source phase completion remain open. The [ledger](3d-binding-first-turn-results.json) and [figure](3d-binding-first-turn.png) preserve the outcome rather than promoting numerical completion into garment acceptance.

The [two-timestep ledger](3d-cuff-temporal-results.json) and [figure](3d-cuff-temporal-results.png) now preserve the half-step comparison and independent review. The older hold and frame/contact artifacts remain unchanged. Reproduction and scope are in [solver evidence](3d-solver-feasibility.md#two-timestep-sensitivity).

The next construction input is now [explicitly phased](3d-solver-feasibility.md#source-cuff-construction-phases--2026-09-23): five fabric instances, both binding prerequisites, 40 sampled source constraints and a reserved shell attachment opening. The phase plan records its research layer-order policy and source dependency refinements; nothing has executed. Eighteen focused tests pass on macOS and Linux ARM. The fresh ignored input `.planning/solver/reviewed-cuff-construction-left-v1/unit.json` contains 227 vertices and 366 triangles, with captured generating source.

[Compliant material controls](3d-solver-feasibility.md#compliant-source-material-controls--2026-09-23) now supply source-triangle pulling, explicit engagement/release, force and discrete-work accounting, rollback, capture and independent gripper replay. The [retained two-triangle contact diagnostic](3d-material-gripper-contact-results.json) completes and replays eight states; it remains synthetic and unaccepted. Independent precision review repaired force, directional-energy and torque cancellation. The captured sewing integration described below adds per-member activation and engagement work while preserving every source row. Construction-specific binding/turning paths must also preserve material-side, interfacing, continuous-seam and opening-passage gates. Generic pulling controls do not execute those phases.

The [captured seam controls](3d-solver-feasibility.md#captured-seam-controls--2026-09-23) now connect complete source-row binding, explicit target/activation input, adaptive sampling and rollback, durable work, CLI capture and independent sewing replay. The cuff profile rederives all forty rows on its compatible numerical runtime from captured engine dependencies. Raw uncaptured activation still rejects. Next implement construction-specific control paths and milestone witnesses while preserving the reserved opening, continuous seam coverage, source/material sides and interfacing decisions. No construction phase has executed.

The seam-activation primitive checkpoint passes **565 full Linux numerical/supervision tests in 201.509 seconds**, including 51 new cases. Documentation and whitespace checks pass; application and deployment scope remain unchanged.

The captured seam-control integration subsequently passes **628 full Linux numerical/supervision tests in 268.099 seconds**, including complete five-fabric cuff capture and independent combined-control replay. The final namespace hardening and focused rechecks are recorded in the [review](../reviews/3d-cloth-contact.md#current-verification-boundary). These numerical controls still do not execute a binding or turning phase.

The next [sampled seam-path gate](3d-solver-feasibility.md#sampled-sewing-path-bounds--2026-09-23) adds optional exact temporal distance-error proof to replay. It requires explicit tolerance and captured scalar-distance controls, rejects knot-crossing intervals, and retains the distinction between sampled temporal bounds and continuous spatial stitching. Construction-specific attachment and motion still need their own source-preserving inputs and observations.

This final checkpoint passes **660 full pinned Linux numerical/supervision tests in 315.871 seconds**, including the final namespace fix and thirty new seam-path/CLI tests. Twenty-seven focused sweep tests also pass on macOS. The earlier checkpoint counts remain historical, not additional current tests.

The final gripper checkpoint passes **514 full Linux numerical/supervision tests in 203.919 seconds**. A preceding run exposed two stale unrestricted mock fixtures; only their declared optional solver features changed, and original guard assertions remain intact. The focused 61 gripper checks and 24 journal checks overlap the full discovery and must not be added to its total. See the review for failure logs and scope. No application release checks or deployment were performed.

- Repository: `erhuve/sew-computer`; remote development branch: `physical-path-certificate-sep19`.
- Current macOS continuation uses the isolated `/private/tmp/sew-computer-3d-directory-scan-repair` checkout on `codex/3d-directory-scan-repair`, based on the verified profiling checkpoint `7bf8c52`. The original study remains in `/private/tmp/sew-computer-3d-engine`; its failed runs and later isolated checkpoints are preserved. The live `/Users/pastel/sew-computer` checkout is not edited or built.
- Historical isolated research checkout on Zo: `Code/sew-computer-3d-engine`, relative to the workspace root. The live service checkout is `Code/sew-computer`; do not edit or test-build it for this work. Historical README/local AGENTS statements that this research checkout backs production are stale; follow the workspace routing index and verify service configuration before any release.
- All implementation and evidence through `1db01e6` were already committed and pushed when this handoff began. The fetched `origin/main` is an ancestor of this development branch. Continue the development branch rather than starting from main and losing the research changes.
- Nothing in this handoff authorizes deployment. Application 3D remains **placement inspection**. Research schedules are not assembled garments, material drape or sewing/fit validation.
- Read the [active implementation plan](../plans/pattern-derived-3d-engine.md), [solver evidence](3d-solver-feasibility.md), [foundation review](../reviews/3d-engine-foundation.md), [inspection review](../reviews/3d-inspection.md) and [normative contract](../design/project-contract.md). This checkpoint summarizes their current state and does not supersede them.

## What is implemented

Source-derived cut meshes retain pattern dimensions, physical-piece identities, original parent triangles and interpolation mappings. Interior seam anchors survive allowance-crease subdivision. Experimental sewing includes fixed vectors, scalar distances and material-normal offsets; the latter include reactions on their defining triangle. Signed hinge actuation adds external fold torques without changing elastic rest geometry.

The current cuff diagnostic combines shell/facing sewing and outer-allowance folding on one 66-vertex, 96-triangle cloth state with twenty seam registrations. Captured stages preserve positions, velocities, seams and contact through transitions and rejected-step retries. Supervised execution retains bounded attempts and immutable inputs; replay reconstructs residuals and checks saved-state integrity, endpoint intersections and continuous-contact certificates.

| Commit | Verified increment |
| --- | --- |
| `234a5b4`, `530a90f` | Source-preserving allowance creases and three replayed signed/near-closed controls |
| `9a6160d` | Captured staged sewing/folding with state continuity |
| `c3b9b76` | Combined source cuff and remapped anchors on the allowance mesh |
| `99a7456` | Exact normal-seam curvature in the guarded search, with existing fallback |
| `1db01e6` | Six-control outcome ledger, geometry figure and remaining failures |

Key entry points: `scripts/solver_assembly_schedule.py`, `scripts/solver_cuff_sequence.py`, `scripts/solver_crease_mesh.py`, `scripts/solver_normal_sewing.py`, `scripts/spike-cuff-sequence-input.py`, `scripts/spike-contact-continuation.py` and `scripts/replay-rest-filtered-continuation.py`.

## Evidence and limitations

The [six-control ledger](3d-cuff-sequence-results.json), [saved-geometry figure](3d-cuff-sequence.png) and [detailed outcome table](3d-solver-feasibility.md#combined-cuff-sequence-outcomes) are committed. All six controls remain `accepted: false`.

- Recorded verification: **386 numerical tests**, a later overlapping seven-test normal-sewing run, and **11 separate harness tests**. These are historical checkpoint results, not newly rerun release checks for this documentation handoff.
- Historical replay verified **461 saved states and 251,106 exact-rational contact-path proof leaves**, including accepted prefixes of failed runs. Both endpoint intersection oracles passed every saved state. This does not certify global layer order, turning or garment quality; replay also shares some force implementations and is not independent model review.
- The completed 0.5 mm-offset, 1.2-radian control takes **39.01 CPU seconds** with exact seam curvature versus **398.86 seconds** with the prior metric. Adaptive histories differ; final geometry differs by **32.925 mm even after rigid alignment**. Do not describe this as equivalent-result acceleration or temporal convergence.
- Facing folds miss targets: measured angles span about **0.08–84.33 degrees** against a **68.75-degree** target; maximum edge stretch is **3.66%** in the completed exact-curvature control.
- Tight **0.11 mm** controls exhaust **600 CPU seconds**, stopping at 37.5% or 35.15625% with the new metric. Stronger wide-gap actuation reaches 94.140625% before exhausting 256 attempts, with up to **12.29% stretch**. Stronger actuation is not an accepted fix.

## Remaining work, in order

The complete material-path maps expose deformation between the five samples, and generic changing-cable integration supplies ordered parameter/motion work with guarded retries. The pose × activation comparison isolates a major original contact transient. Tightening the numerical force criterion now reduces conditional balance defects without resolving the dominant early timestep velocity discrepancy. Resolve contact/control dynamics with declared temporal accuracy and dissipative semantics, then establish spatial sensitivity. Resolve the physical joint model at creases and corners separately: adjacent one-sided normals can make positive-offset registration incompatible, while the tension-only alternative permits collapse, wrong sides and rotational freedom. Make source face/sign/textile choices and reference measure explicit, retaining every original point row and conversion defect. Joint space/time distance verification remains open; low integrated energy or a fixed-state distance pass does not prove an attachment or contact-safe motion. Do not silently smooth normals, retarget anchors, normalize coefficients or add the old point stiffness a second time.

Generic coupled controls now extend through 1,024 intervals, with full saved-state review and no tolerance changes. The finest-pair discrepancy already starts during the first ramp, before upper-fold engagement; release-only diagnostics would miss it. Next isolate and resolve these early contact/control transients while preserving the physical inputs, actual partitions and complete work. Declare the intended temporal accuracy and dissipative model before interpreting low speed as settling. Use the completed static source-frame descriptor to make explicit construction-specific frame, offset-sign and textile-side choices, then validate the prospective joint model. Preserve every original row; neither the declarations nor their static verification change refined-source admission or install executable frames.

1. **Establish temporal/spatial accuracy with bounded, observable execution.** Preserve the changed exact-feature model and the three CPU-limit failures in its original four-case study. A/B have no returned histories; the original fine-reference process lacks a successful terminal audit. The successful supplemental saved-reference audit remains separate, and its 2,048/4,096 comparison exceeds both original state targets. Local energy-allocation tests on the coarse stored path expose early-transient pressure and do not reconstruct adaptive acceptance. Contact-work caching, retained execution, bounded signal/export and independent raw prefix admission are integrated here. The buffered resource witness establishes export and separate audit feasibility for the declared 326 MB / 3,072-trial shape. It does not establish arbitrary larger payloads or the old 757 MB report schema. The retained response/path adapter now passes stopped original-input controls through active contact, including all valid alternatives and unassessed tails. The prospectively declared cached A study now retains a 6.7578125 ms prefix at its wall limit, with all 2,318 valid candidates separately audited. Its tighter B case remains unexecuted. Matched three-call and 128-call profiling now preserves exact histories; its early workload places 48.33% of operation CPU in solver steps and 23.32% in energy transitions. A later function-level profiling attempt stops at its wall limit after 119 calls and rejects its measurements after a stop exception in the clock callback. The subsequent lighter targeted timers complete the same 128-call workload and preserve complete matched histories. Exact contact construction accounts for 18.06% of this early operation, while logarithm series account for 1.36%; the remaining unmeasured work is not assigned to a function. The allocation guard and artifact-inventory repair now pass the 1,590-test full suite. Six matched early128 observations preserve complete histories but have mixed paired timings and only a 0.719% median operation CPU reduction; consistent acceleration is unestablished. Subsequent controller profiling finds only 1.019% of operation CPU in its eight selected functions. The latest adaptive-validation pair explains 91.187% of the broad remaining interval, dominated by the isolated helper and the fresh numerical work it calls. Next distinguish immutable effective-control construction from response/work evaluation there, with contact validation attributed explicitly, before choosing a measured optimization and returning to the full numerical budget. Preserve strict input/feature/budget checks, fresh native endpoint validation, contribution identities, memory bounds and exact-history controls. Broader mutable native bucket reuse remains a separate proposal. Preserve 1e-8 N stationarity, original position/native-velocity goals, physical inputs, complete reservation costs, geometric path guards and all failed evidence. Include the original preloaded control and released tail; establish spatial sensitivity and construction-specific joints before freezing acceptance profiles. Raw export remains best effort; no hard-kill persistence or resume guarantee follows.
2. **Execute and validate source construction controls.** The static combined declaration preserves the five-held/thirty-five-pending sewing controls, compliance, source rows, native hinges, placement and retry grid. Resolve construction-specific joint/frame/layer choices; complete captured integration and independent replay after the pending entry-point authorization. Then resolve facing hinge tracking, seam/frame reactions and contact interactions on the source cuff. Any additional material anchors need separate source justification. Require measured target error, seam error, layer-side behavior and deformation, not just completed time or low residual; neither the static declaration nor the generic two-layer control establishes construction.
3. **Complete one cuff construction.** Execute closure, allowance/corner treatment and actual turning through the declared source attachment opening in one continuous replayed sequence. Current parallel-layer folds do not turn the cuff. Preserve the opening and all shell/facing roles; then validate sleeve attachment.
4. **Implement remaining garment operations.** Binding wraps and open sleeve edges, collar/stand turning and layered attachments, gathers/multi-way junctions, localized closures, and explicit interfacing treatment must execute from source semantics. Reconcile all 24 fabric instances and six unresolved interfacing roles before claiming a complete shirt.
5. **Validate full-shirt assembly and drape.** Demonstrate closure, settling, strain/contact/source fidelity, repeatability and bounded execution across the declared size/component fixtures. Body contact and calibrated material behavior require their own evidence. No current control establishes these gates.
6. **Integrate and review the accepted scope.** Only after solver acceptance, connect assembly output to the private worker/viewport with revision/runtime identity, cancellation/recovery, resource limits and privacy/export rules. Run independent adversarial review and applicable application/engine/browser/build checks, then follow [deployment procedures](../deployment.md) with schema-compatible rollback and explicit release authorization.

The placement viewer and asynchronous interpretation fix are a separate, narrower release candidate. At `f6680f5`, the recorded checks passed 104 application, 34 engine and 23 browser tests plus typecheck/build. That historical result does not certify today's branch or authorize deployment. A placement-only release needs its own scope decision and fresh checks; it does not require pretending the simulation gates are complete.

## Resume and reproduce

Fetch before starting and before every authorized push; preserve concurrent changes. Commit verified increments locally. Current automatic approval review has rejected both the prepared refined capture/main-replay edits and GitHub publication pending specific user authorization. Those requests remain pending; do not infer approval from a continuation, configured remote or repository instruction to push. After explicit authorization, push only the authorized payload to the authorized branch. Use the configured author and committer identities, with `Co-authored-by: hatsunemiku <hatsunemiku@zo.computer>` in the commit body.

The ignored research runtime is `.planning/solver/newton-venv`; its pinned dependencies are `scripts/solver-contact.requirements.txt` (which includes `solver-spike.requirements.txt`). On a new machine, create a separate virtual environment and install those requirements. The upstream pattern engine needs its separately provisioned locked checkout; see the root README and `services/engine/source-lock.json`.

The September 23 Linux ARM controls used Python 3.13.15 with the same package pins. IPC Toolkit 1.6.0 was built from upstream tag commit `478876f30bf8ea768772dd8983c26a1a801ad976`; the available source distribution failed to build on that host. A separate Python 3.13 Linux environment with CMake, a C++ toolchain, Git and Ninja can install `scripts/solver-spike.requirements.txt`, then `git+https://github.com/ipc-sim/ipc-toolkit.git@478876f30bf8ea768772dd8983c26a1a801ad976`. Record the resulting runtime identity rather than assuming a wheel and source build are equivalent. The new ledger includes the measured platform and package versions. The exact upstream cause of the native LBVH omission remains open; explicit HashGrid and independent candidate-coverage verification are required for these controls.

Run from the isolated repository root. The complete numerical discovery and supervised continuation CLI require Linux (`/proc`, `prctl` and process-resource enforcement). Individual numerical and source-generation tests also run on macOS; that does not verify the Linux worker lifecycle. Keep Warp's cache in the isolated writable research directory. For focused checks:

```sh
export WARP_CACHE_PATH="$PWD/.planning/solver/warp-cache"
OPENBLAS_NUM_THREADS=1 OMP_NUM_THREADS=1 PYTHONPATH=scripts .planning/solver/newton-venv/bin/python -m unittest test_solver_temporal_control test_solver_temporal_global
OPENBLAS_NUM_THREADS=1 OMP_NUM_THREADS=1 .planning/solver/newton-venv/bin/python -m unittest discover -s scripts -p 'test_solver_stationarity.py'
OPENBLAS_NUM_THREADS=1 OMP_NUM_THREADS=1 .planning/solver/newton-venv/bin/python -m unittest discover -s scripts -p 'test_solver_continuous_normal_sewing.py'
OPENBLAS_NUM_THREADS=1 OMP_NUM_THREADS=1 .planning/solver/newton-venv/bin/python -m unittest discover -s scripts -p 'test_solver_normal_sewing.py'
OPENBLAS_NUM_THREADS=1 OMP_NUM_THREADS=1 .planning/solver/newton-venv/bin/python -m unittest discover -s scripts -p 'test_solver_cuff_sequence.py'
OPENBLAS_NUM_THREADS=1 OMP_NUM_THREADS=1 .planning/solver/newton-venv/bin/python -m unittest discover -s scripts -p 'test_prepare_cuff_source.py'
OPENBLAS_NUM_THREADS=1 OMP_NUM_THREADS=1 .planning/solver/newton-venv/bin/python -m unittest discover -s scripts -p 'test_solver_*.py'
python3 scripts/check_docs.py
git diff --check
```

For the combined diagnostic, extract sewing with `spike-cuff-sewing-input.py --normal-offset-frames` and folding with `spike-cuff-fold-input.py --crease outer-allowance --target-angle-radians 1.2`, using the same parent canonical source. Combine their canonical files with `spike-cuff-sequence-input.py --sewing-input ... --fold-input ... --crease-frame-region body --output ...`. Use each CLI's `--help` and fresh private output directories; never overwrite historical runs.

Use the ledger's per-control `arguments` for exact settings and captured source hashes for historical comparisons. The completed exact-curvature wide-gap control uses the combined canonical/placement inputs with:

```sh
OPENBLAS_NUM_THREADS=1 OMP_NUM_THREADS=1 .planning/solver/newton-venv/bin/python scripts/spike-contact-continuation.py --canonical /absolute/path/to/new-input/canonical.json --placement /absolute/path/to/new-input/placement.json --output /absolute/path/to/new-run --assembly-schedule --fold-actuation --sewing-mode normal-offset --contact-model rest-filtered --ccd-profile temporal-separation-tight-inclusion --activation-distance-m .001 --minimum-distance-m .0001 --pressure-pa 10000 --target-fraction .25 --step-seconds .064 --subdivisions 64 --max-evaluations 100 --max-attempts 256 --max-depth 8 --cpu-limit-seconds 480 --wall-limit-seconds 720
OPENBLAS_NUM_THREADS=1 OMP_NUM_THREADS=1 .planning/solver/newton-venv/bin/python scripts/replay-rest-filtered-continuation.py /absolute/path/to/new-run
```

These are experimental settings, not acceptance thresholds. Changes to source, targets, material, contact or budget must remain explicit in new evidence.

## What a Git clone does not contain

The six `.planning/solver/cuff-sequence-*-sep23` run directories named in the ledger are present on this Zo at handoff. They hold local research artifacts; `.planning/` is ignored. The parent source used in documented extraction is `.planning/solver/fold-barrier-shirt-v1/canonical.json`. A Git clone carries code, ledgers and figures, **not** the virtual environment, private parent source, captured inputs, journals or full saved trajectories. Hashes identify those artifacts but cannot recreate their bytes.

For a handoff to another machine, arrange an authorized private transfer of the required parent input and captured run directories, or regenerate synthetic fixtures using the documented pipeline and record new digests. Retain numerical source snapshots when reproducing the prior-metric controls; current code alone will use the new metric. Do not commit private measurements, production data, credentials, external assets or runtime files to make a clone self-contained. Full replay requires the original saved artifacts; the committed evidence summaries alone are insufficient.

To generate a fresh synthetic cuff with the pinned research runtime:

```sh
OPENBLAS_NUM_THREADS=1 OMP_NUM_THREADS=1 .planning/solver/newton-venv/bin/python scripts/prepare-cuff-source.py --output .planning/solver/new-synthetic-cuff-source
```

Pass its `canonical.json` through the existing sewing and fold extraction commands above. This source has new provenance and does not reproduce the historical canonical bytes. It preserves the sleeve-attachment opening but does not execute the selected perimeter operations' external dependencies or demonstrate turning. The generator and complete sewing/fold source-remapping pipeline have six independent regression tests in `scripts/test_prepare_cuff_source.py`.
