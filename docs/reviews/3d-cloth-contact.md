# Allowance cloth and research contact review

**2026-09-17 · Independent adversarial review · Research foundations only**

Reviewed `services/engine/cloth_domain.py`, `scripts/solver_contact_filters.py`, their tests and the research harness integration. These modules do not complete the [dedicated engine plan](../plans/pattern-derived-3d-engine.md), enable assembled output, or validate material drape.

## Numerical continuation review · 2026-09-23

The [coupled sewing/fold/contact increment](../research/3d-solver-feasibility.md#coupled-sewing-fold-and-contact-mechanics--2026-09-23) exposes and repairs a final-publication gap: after valid step diagnostics, a work helper could alter the sewing report or omit/replace required sewing work fields while fold checks still passed. Exact sewing model snapshots, isolated work inputs, fresh control/diagnostic checks and complete finite work fields now precede every accepted journal outcome and callback. Regression attacks preserve the first accepted prefix. One compatibility check caught an incorrect name for the newly authored gripper momentum field; the implementation was corrected and the existing six activation/adaptive tests pass. An initial publication-test fixture also needed valid integer normal sides and negative-anchor faces; its failed logs remain distinct from the final results. No tolerance was relaxed.

Separate actual cloth tests exercise every coupled force contribution, pending-target independence, unchanged rest data, fold release and a solved-then-rejected trial followed by correctly resampled retries. Two retained 64 ms synthetic controls repeat all sixty-four raw states byte-for-byte after the repair. Independent review verifies contact/triangle/hinge interval coverage and work with disclosed shared native/mechanical helpers. The wider contact buffer is already active initially; it gives 32 contact-active states and 0.127302 N peak held-seam force, but its within-interval seam error reaches 0.381 µm and final speed remains 14.01 mm/s. The narrower case finishes at 68.05 mm/s. Neither is settled, all-controls-free, a core-contact encounter, a continuous spatial seam or a garment phase. Capture/main-replay entrypoints remain unchanged.

Current mixed-control verification: **1,002 full pinned Linux numerical/supervision tests pass in 596.167 seconds**, including **fifteen new cases** (six actual-mechanics and nine publication-validation tests). All fifteen also pass on macOS and Linux in focused runs; these counts overlap the full discovery. The [mixed-control ledger](../research/3d-mixed-control-results.json) records complete logs, source and artifact identities, earlier failed development checks, and the independent saved-state review. The full log is `.planning/solver/mixed-control-full-numerical-sep23-v1.log`. This offline increment changes no application or deployment behavior.

The [combined sewing/fold static declaration](../research/3d-solver-feasibility.md#combined-source-sewing-and-fold-declarations--2026-09-23) preserves all forty ordered original/numerical row hashes and unchanged reference targets/compliance, with five held and thirty-five pending samples. Independent exact coefficient arithmetic finds nonzero differences in all five held anchor vectors; the smallest component is about 1.54e-33 m. Tests and directed review retain those differences, original local prefixes at current canonical offsets, raw numeric/signed-zero representations, phase membership and source identity. A separate standard-library auditor reconstructs the full declaration and rejects altered witnesses even when selector IDs are unchanged. Joint parameter sampling covers all 16,385 original grid fractions; this is not a motion or force test. Reference extras supply provenance only, and releasing fold controls leaves the sewing samples held. The capture/main-replay entrypoint proposal remains unapplied; no source-admission expansion, refined motion or construction acceptance is included.

This continuation found defects beyond the earlier review scopes. Historical test counts below remain historical; neither those results nor the fixes here establish accepted cuff construction, full-shirt assembly or deployment readiness.

Previous combined-declaration checkpoint: **987 full pinned Linux numerical/supervision tests pass in 601.119 seconds**, including **24 new cases** (twelve producer and twelve independent-auditor tests). All twenty-four also pass on macOS using fresh native fixtures; this does not certify cross-runtime source bit identity. The retained Linux declaration matches independent joint parameters at all **16,385 grid fractions across sixteen hinges and forty sewing rows**. Directed review checks all row identities/hashes and exact held-row witnesses, including tiny differences and four rejected forgeries. The audit reproduces byte-identically in an isolated standard-library-only process. Complete logs and artifact identities are in the [combined-control ledger](../research/3d-binding-sewing-schedule-results.json); the full log is `.planning/solver/binding-sewing-schedule-full-numerical-sep23-v1.log`. No refined motion, source-admission change or application release is included.

### Binding hold, refinement and velocity observations

The subsequent [source-bound temporal declaration](../research/3d-solver-feasibility.md#source-bound-temporal-fold-declarations--2026-09-23) remains static. Independent review requires complete source/native/placement identity, every hinge and rail sign, strict request/retry-grid admission and a separate record of observed starting angles. Reference targets are excluded from motion controls. Existing grippers conflict with the constructed internal-fold reference; changing the fixed-complement attachment sign also produces a sleeve intersection. Neither constructed poses nor parameter sampling certify a free coupled trajectory. No source motion, capture admission or garment completion is added.

Previous checkpoint verification: **963 full pinned Linux numerical/supervision tests pass in 487.079 seconds**, including **25 new cases** (twelve producer and thirteen independent-auditor tests). The new cases also pass on macOS using freshly generated native fixtures; this does not establish cross-runtime source bit identity. The retained Linux request matches independent parameter calculations at all **16,385 grid fractions across sixteen hinges**. Separate review passes 220 small-grid comparisons and retains fifteen activation-underflow rejections. The saved audit reproduces in a standard-library-only process. Log: `.planning/solver/binding-fold-schedule-full-numerical-sep23-v1.log`. These checks execute no refined motion and do not certify an application release.

The subsequent [generic fold-control integration](../research/3d-solver-feasibility.md#guarded-fold-activation-and-release--2026-09-23) retains all declared hinge guards after release and checks complete adaptive control/work reports before journal acceptance. Independent actual-geometry probes isolate initial, optimizer-only and physical-only branch crossings with zero material bending and valid triangle paths; every unsafe hinge path rejects. Review repaired silent raw-state narrowing, inconsistent top-level actuator flags/energy and post-work report mutation. Failed or mutating work helpers preserve the accepted prefix and publish no candidate state. Final raw candidate checks, recipe identity, total-work fields and convergence diagnostics were inspected before freezing the integration.

Independent saved-state review verifies input/script/helper/state hashes, native hinge membership, original-fraction controls, rounded stiffness coefficients and target-first parameter work for all 32 steps. Independently written membrane and hinge force formulas give a maximum residual of **8.92531e-7 N**, agreeing with the retained reconstruction within 1.843e-14 N. Exact rational checks establish exhaustive swept-AABB candidate coverage, all **128 contact support leaves**, **128 triangle intervals** and **64 declared-hinge intervals**, including released hinges. Contact force evaluation still uses the pinned native adapter; stable motion-energy differences retain shared helpers. This is not a wholly independent mechanical implementation or supervised capture.

The three nonzero contact states contain interlayer activation-buffer stencils only, at separations of about 1.09846–1.09973 mm, just inside the 1.1 mm core-plus-activation boundary. Their peak contact-gradient component is **1.15897e-4 N**. The separate 0.1 mm core-path proof has 128 intrapanel edge-edge leaves; exact swept-AABB coverage excludes cross-layer core candidates. Thus the control exercises a small interlayer contact response, not a loaded wrap or contact-core encounter. Static artifacts verify the recorded rest arrays and final adapter snapshot; the generator checks those arrays after each step, but the artifact review cannot independently prove that historical in-memory values were never transiently changed.

The preceding generic fold integration passed **938 pinned Linux numerical tests in 403.734 seconds**, including **28 new tests** (seven global integration, eleven adaptive control and ten energy accounting). Focused checks also pass on macOS and Linux. The retained full log is `.planning/solver/controlled-fold-motion-full-numerical-sep23-v1.log`; the motion ledger records source, state, review and test identities. This is offline generic numerical evidence, not refined garment execution or application release verification.

No refined garment input is executed. Captured admission/main replay remain unchanged; the retained synthetic callback records do not claim supervised execution or garment acceptance.

The [explicit fold-control primitives](../research/3d-solver-feasibility.md#explicit-fold-schedules-and-mechanics--2026-09-23) now retain complete ordered hinge identity while sampling independent target and activation schedules. Read-only review checks discrete target-first work against 250-digit Decimal for 100 cases and exact telescoping over a 32-jump closed control loop. Released actuator energy is reported separately from dissipation. The common rounded stiffness-times-activation coefficient is used throughout mechanics and work; coefficient-product residuals are not physical work.

Review found and repaired original-integer coercion, mutable evaluator drift, representable force lost through intermediate underflow, nonsymmetric Gauss–Newton contraction and shared-vertex force cancellation. Independent exact sparse-product checks match all derivative entries in three retained counterexamples. Angle review additionally found false motion under exact uniform scaling; a general rationalized fallback now retains small angular signals on larger shape changes. Independent forced-binary64 checks match 354 admitted cases against 500-digit Decimal, including 122 zeros; six nonzero increment-underflow cases reject. These checks are conditional numerical evidence, not an exact-real force/angle theorem or continuous geometry certificate.

The complete pinned Linux numerical/supervision suite passes **910 tests in 394.011 seconds**, including all forty-four new cases (19 mechanics, 11 schedule and 14 angle-increment tests). The new cases also pass in focused macOS runs; these counts overlap the full Linux discovery. Log: `.planning/solver/controlled-fold-full-numerical-sep23-v1.log`. This verifies standalone offline primitives, not guarded motion, construction or an application release.

This increment introduces no captured source controls, global/adaptive integration or executed cloth motion. Inactive force masking preserves identity and does not replace complete cloth/contact/triangle guards. The rejected refined runner/main-replay edits remain unapplied. The [ledger](../research/3d-controlled-fold-results.json) records probe/source identities, tests and explicit limits; application output classification and deployment status remain unchanged.

Final independent evidence review verifies all nineteen retained artifact identities and the pinned image ID, recomputes all 100 work cases and 32 closed-loop jumps, and reconstructs the ledger byte-for-byte. Seven execution/legacy files remain unchanged from `bee63d2`. No remaining blocker is found within the stated standalone arithmetic scope.

The [refined rigid-placement descriptor](../research/3d-solver-feasibility.md#source-bound-refined-placement--2026-09-23) now separates unchanged source coordinates from explicit pose coefficients and once-rounded world coordinates. Independent review confirms the complete edge-error and raw-lineage commutation identities, including the translation term for non-unit coefficients. A concrete exact Gram-boundary regression rejects an excess of 2⁻⁹⁴ that an ordinary binary64 dot check hides. One-ULP position, determinant, lineage and separating-plane forgeries reject. A standard-library auditor independently reconstructs raw stages, all canonical indices and all ten pairwise plane gaps; thirteen audit tests pass on both platforms.

The new uniform arithmetic policy differs from historical BLAS placement at only binding local vertex nine, by at most 5.551115e-17 m. All original vertices match when compared under the same new policy. Existing gripper targets remain unchanged, and the tiny resulting spring-energy differences are retained. The requested geometric gap is explicitly distinct from the configured contact core; static pairwise separation alone cannot establish within-instance contact, a staging path, integrated motion or construction acceptance.

Separate fixed-profile static review admits the complete 245-vertex/398-face placement with 0.1 mm activation/core, zero filtered pairs and zero contact energy/gradient. Both nonadjacent surface-intersection oracles pass. Independent exact broad-phase coverage checks 38,580 comparisons and accounts for all 1,018 native candidates; separately retained rational projection inequalities verify all 1,018 assigned stationary gaps. Exact stationary triangle verification covers all 398 faces. The retained report binds native libraries, Python and twenty-one loaded repository helpers. No solver step, loaded-contact response, sewing/fold equilibrium or construction path is claimed. Independent source review also verifies three additional non-unit arithmetic cases against 250-digit Decimal calculations and confirms that signed-zero/rotation/parent/witness attacks change meaningful inputs.

The frozen placement checkpoint passes **866 full pinned Linux tests in 407.172 seconds**, including eleven producer and thirteen independent-audit cases. All twenty-four new cases also pass on macOS. Documentation and whitespace checks pass, and the two proposed capture/main-replay entrypoints remain byte-unchanged. This checkpoint changes offline research helpers and evidence, not application output classification or deployment status.

Final independent v2 evidence review verifies all eighteen input/raw/probe identities, four direct helper hashes, generator identity, twenty source-closure hashes and twenty-one static-helper hashes. Embedded primary/static artifacts and runtime-summary reductions match their saved bytes. Every one of the 1,018 stationary projection witnesses is independently recomputed from saved coordinates and canonical/native connectivity, retaining strict 0.1 mm margins and candidate identity. The five source seam translation terms and non-unit counterexample also rederive exactly. A fresh pinned Linux rebuild reproduces the source, descriptor and source/seam/placement/gripper audits byte-for-byte; all 2,742 installed package files, 521 loaded external modules and Python executable/shared-library hashes match. No remaining blocker is found within the declared static scope.

The [source-bound fold controls](../research/3d-solver-feasibility.md#source-bound-binding-fold-controls--2026-09-23) derive body/allowance regions from oriented source topology and bind all 638 native edge rows before selecting sixteen rail actuators. Independent review checks all 384 rail-hinge permutations on both platforms, signed static rotations, full-cut region continuity and balanced free-region reactions. A separate 160-digit Decimal oracle checks segment lengths and stiffness products; the standard-library auditor independently uses exact rational bisection. All native single-sided winding reversals, missing/duplicate edges and forged boundary incidence reject. Density weights external line actuation, not textile bending or material calibration.

The sequential frame probe exposes a concrete construction hazard: reusing the flat finish axis after the attachment-body rotation distorts source edges by about 54.743 µm. Transporting the axis retains all source vertices/faces and reaches both declared relative angles in a directly constructed pose. The four other instances remain unchanged. These fixtures execute no dynamics and do not have validated whole-unit placement or contact. One initial producer tamper case wrote the existing rational numerator; the test now changes it and explicitly checks that every attack modifies the input before asserting rejection. No admission tolerance was relaxed.

The frozen fold-control checkpoint passes **842 full pinned Linux tests in 379.840 seconds**, including all ten new producer/native-primitive and twelve independent-audit cases. All twenty-two also pass on macOS. Documentation and whitespace checks pass. These checks do not execute the unapplied entry-point proposal, certify contact or change the application's placement-inspection status.

Final independent evidence review verifies all thirteen retained raw/base identities, five direct helper hashes, twenty source-closure hashes and generator identity. A fresh pinned Linux rebuild reproduces the source, complete 638-edge native topology, fold descriptor, audits and current primitive binding byte-for-byte. Saved-pose angles/energies and initial force/torque diagnostics reproduce exactly; separate standard-library geometry and 200-digit Decimal lengths confirm the measured transport result and wrong-axis distortion. Both shared-chain continuity claims and the four unchanged instances pass. No remaining blocker was found within this static correspondence scope.

The subsequent [gripper migration](../research/3d-solver-feasibility.md#binding-gripper-migration--2026-09-23) keeps original control recipes intact while deriving source-bound child anchors and rebuilding unchanged-instance indices. Independent read-only review reproduces all three actual control bindings and the sleeve 200→232 counterexample; retaining the old sleeve index silently moves its material point despite passing generic input checks. Exact coefficient pullback and stored-coordinate reconstruction are recorded separately. An independent standard-library audit uses determinant solves and midpoint rounding checks, without importing the producer or numerical stack. Review strengthened its per-stage parent-support checks after an intermediate-parent mutation exposed an incomplete audit. These are correspondence checks; they do not establish an executed refined trajectory or equivalent forces.

The gripper checkpoint passes **820 full pinned Linux tests in 361.034 seconds**, including eleven producer and thirteen independent-audit cases. All twenty-four new tests also pass on macOS. Independent evidence review verifies all fourteen source/artifact identities, five direct helper/verifier hashes, twenty source-closure hashes, generator identity and original capture linkage. The first ledger omitted the independent seam-auditor digest; the regenerated v2 ledger adds it while preserving every numerical artifact byte. Repaired-field control, lineage and material-point attacks reject. Documentation and whitespace checks pass; no entry-point edit or deployment is included.

The subsequent [anchor-remapping implementation](../research/3d-solver-feasibility.md#binding-anchor-remapping--2026-09-23) distinguishes immutable original seam operators, ideal exact child coefficients and explicitly rounded numerical coefficients. Separate tests and an independent raw-stage audit retain non-unit source sums, reject negative geometric candidates without clipping and check pullback error bounds. The new source profile rebuilds global indices without relabeling original template geometry. The sewing binder and independent verifier reject profile/field downgrade and undeclared normal-frame semantics. The proposed runner/main-replay wiring remains unapplied following automatic approval rejection; this work does not establish a captured refined trajectory.

The earlier seam-remap checkpoint passes **796 full pinned Linux tests in 422.720 seconds**, including all forty new cases at that stage. Independent retained-evidence review confirms the actual base/derived byte hashes, current helper closure, all twenty-three candidates, maximum coefficient residual of **1.18178e-17** and complete forty-row correspondence. The independent audit imports no producer, engine or NumPy module. These tests do not execute the unapplied entry-point proposal.

The [longer-hold comparison](../research/3d-solver-feasibility.md#extended-binding-hold--2026-09-23) retains a negative result after all four runs pass independent replay. Complete numerical snapshot identity and every saved position/velocity through 384 ms match. The additional hold leaves final speed near 0.478 mm/s and increases the released-tail angle range; small sampled seam errors do not establish settled construction. Sixteen focused tests attack policy fields, changed source/settings, Boolean times, release activation and prefix artifacts including repaired hashes.

Independent review of the non-executable [binding crease descriptor](../research/3d-solver-feasibility.md#binding-crease-preprocessing--2026-09-23) confirms its two full-allowance crease chains, unchanged original vertex prefix, original-parent support and strict whole-descriptor rederivation. Eight focused tests pass on both platforms; thirty-four additional mutations reject. Exact binary-input area checks retain tiny nonzero residuals, so this does not certify exact-real domain equality. Remapped seams, frames, grippers, capture and numerical execution remain pending.

The separate rigid-velocity primitive initially passed thirteen tests while admitting two numerical counterexamples: about 35% kinetic underreporting under extreme mass/velocity scales and false zero speed from norm underflow. The repaired helper explicitly rejects subnormal normalized masses, uses compensated original weighted sums and stable speed norms. Sixteen tests pass on both platforms, including the retained failures, and independent rational-oracle checks confirm nearby admitted ranges. Its measured reconstruction/orthogonality residuals are observations, not dissipation, continuous rigid-motion or acceptance certificates. No material or simulation policy changes are introduced.

Final complete pinned Linux numerical/supervision discovery passes **756 tests in 347.358 seconds** after the fixes. Independent comparison review also reconstructs all 192 prefix states per role from raw artifacts and verifies the current reporter digests. The previous 753-test pass preceded the last three velocity regressions and does not substitute for this final run. Documentation and whitespace checks pass; the application remains placement inspection.

### Contact locality and native parameter identity

The experimental rest-filtered v1 implementation selected one reduced activation/minimum scale from the smallest within-panel rest separation in the entire input. Adding a distant, stationary refined panel could therefore erase the original panel's contact response. This violates locality even when the broad phase supplies a complete candidate set; the rest-filtering partition itself does not drop candidates. Historical v1 refined-mesh controls must not be promoted to model acceptance.

`experimental-rest-filtered-contact-v2` assigns each filtered primitive pair its own fixed rest-distance-derived scale: activation is one quarter of that pair's rest separation, and minimum separation is the smaller of full thickness and that activation. Only identical parameter values share an evaluation bucket. Inter-panel candidates retain full thickness; rest filtering discards no additional nonincident candidate. The profile records membership and parameter hashes, ranges, bucket counts and the rule. This is an explicit model revision, not an equivalent replay of v1.

Independent regressions verify unchanged original energy, forces and curvature when unrelated distant panels are added or refined. A separate 121-candidate sum reproduces grouped energy, forces and Hessians across changing closest-feature reductions. Review also found that mutable native barrier/CCD objects could change the effective model while its profile stayed fixed. Public evaluations now reject such parameter drift, including cached evaluations. The initial **21 rest-filtered tests passed on macOS ARM**; the subsequent Linux run exposed the separate broad-phase defect below.

Closest-feature transitions retain continuous forces and piecewise curvature; there is no unique ordinary Hessian at a transition. Locality does not prove refinement-convergent quadrature, globally uniform sheet thickness, material calibration or acceptable layer order. The regenerated coarse cuff has **zero filtered pairs**, so this locality fix does not itself explain or resolve its fold-tracking failure.

### Linux ARM broad-phase omission

The pinned Linux ARM runtime uses Python **3.13.15**, Linux **aarch64 / glibc 2.41**, Newton **1.6.0**, Warp **1.17.0**, NumPy **2.5.3**, SciPy **1.17.0**, Shapely **2.1.2** and IPC Toolkit **1.6.0** built from the upstream v1.6 tag at commit `478876f30bf8ea768772dd8983c26a1a801ad976`. The first complete Linux numerical discovery selected **415 tests** and reported **29 subtest failures and three errors**. Passing the earlier macOS subset did not establish native Linux correctness.

The native default broad phase omitted required candidates. A small grid produced **79 rather than 121** pairs, a refined grid **1,219 rather than 2,029**, an aligned-layer fixture **seven rather than 15**, and a folded fan **two rather than three**. Explicit `LBVH` reproduced these omissions. `HashGrid`, `BruteForce` and `SweepAndPrune` agreed on the complete candidate sets for these controls. This is a measured failure of this pinned source build on this runtime, not a claim about every IPC Toolkit platform or version. Missing candidates can change forces and invalidate a contact certificate; these outcomes cannot be interpreted as cloth physics.

All native contact, intersection, CCD, swept/temporal candidate, preflight, supervised-CLI and replay paths now explicitly select a fresh `HashGrid` through `solver_ipc_broad_phase.py`, and record `ipctk-HashGrid-explicit-v1` in the contact/path profile. **62 focused Linux contact tests pass**. The corresponding macOS run selected **62 tests: 61 passed and one expected extended-precision test was skipped**. The full Linux rerun is recorded below. The precise upstream implementation/compiler cause of the LBVH omission remains unproven.

The first Linux cuff baseline, captured before this fix, retained **49 accepted integration steps and 45 rejected attempts**, then stopped at **19.53125%** of its requested duration after exhausting subdivision depth. It consumed **208.669 seconds of worker-reported CPU time**. Its ignored `.planning/` artifacts remain a rejected runtime diagnostic; its contact results and stopping point are not evidence about cuff feasibility or settling. The strengthened replay rejects this capture at its expected/proof candidate-count mismatch and creates no verified artifact. The historical six-control ledger is unchanged. Corrected baseline/hold controls are recorded below.

### Independent candidate completeness

`solver_candidate_coverage.py` uses only the Python standard library to construct exact-rational swept endpoint AABBs from captured binary64 coordinates. An inclusive sorted-axis sweep checks every required nonincident face/vertex and edge/edge pair against observed primitive identities. Input, topology and candidate validation reject malformed or nonfinite data; bounded primitive, comparison and candidate counts fail closed. Extra nonincident candidates are allowed. This verifies conservative candidate completeness, not separation itself.

Replay loads captured current verifier bytes before importing historical numerical snapshots, records verifier digests, reconstructs candidates through explicit HashGrid, and requires certificate leaves to cover those primitive identities and intervals. Candidate enumeration order may differ; inconsistent index/identity mappings reject. Six pure coverage tests, six native fixture tests and five later independent adversarial tests cover inclusive boundaries, extreme translation, topology/identity permutations, exact budgets, native reordering and eleven replay-proof corruptions. Native tests compare independent BruteForce results and transformed/scaled/refined/source-cuff fixtures. The known default-broad-phase failure remains a rejected diagnostic.

### Every triangle's physical path

Nonincident contact certificates can pass a motion whose individual triangles collapse between valid endpoints. A two-triangle counterexample rotates each face by 180 degrees under affine interpolation: both endpoint meshes and normal-offset seam residuals are valid, but both triangles collapse at the midpoint. An isolated face need not belong to a guarded hinge.

`solver_triangle_sweep.py` checks every source face on both optimizer segments and proposed physical steps. A bounded, scale-normalized Bernstein calculation fails closed on an unresolved path. Cloth starts from the previous state; the unguarded `lsmr` route rejects cloth triangles. An independent exact-rational replay calculation proves nonzero triangle area throughout each saved affine interval using the exact binary64 endpoint values. It is loaded from the current verifier's captured bytes before historical numerical snapshots enter the import path; its digest is recorded separately.

The **12 focused triangle tests pass**, including off-grid tangencies, mixed safe/collapsing faces, scales from `2^-500` to `2^500`, malformed indices, exhausted budgets and independent-oracle attacks. The runtime numerical guard is not directed interval arithmetic. The exact verifier proves nonzero area, not a physical minimum area, bounded strain, contact clearance or global layer order. Historical saved paths have not acquired this proof merely because the replay tool was updated.

### Portable fold-energy differences

On Apple ARM, `numpy.longdouble` has binary64 precision. Subtracting independently rounded angles lost significant digits in tiny fold-energy updates; two existing tests failed without any changed tolerance. The repair propagates small coordinate increments through edge/normal rotations and evaluates the corresponding barrier difference. Tests now use an independent 65-digit Decimal geometry/angle oracle instead of rounding through the same platform angle calculation.

Adversarial review caught a first-version regression under extreme uniform contraction. The final code restricts incremental evaluation to well-conditioned small changes and uses independently validated endpoint geometry for large changes. **36 distinct focused fold tests pass**, including the reviewer's exact binary rescaling, tiny motion at multiple scales, sign/activation crossings, mixed batches and rejection cases. Simultaneous huge rescaling and an extremely small rotation still has endpoint-rounding limits; arbitrary-transform relative accuracy is not claimed. None of these arithmetic checks establishes material correctness.

### Source cuff and the next construction gate

`prepare-cuff-source.py` creates a fresh synthetic source cuff using the existing shirt compiler, cut-domain mesher and embedded-registration validator. It preserves both physical layers, all twenty perimeter registrations and the open sleeve-attachment edge. Its canonical pattern hash is reconstructable; source bytes are captured before local imports and checked before publication. Runtime/source digests and the unresolved operation dependencies are explicit. Six independent tests verify deterministic regeneration, source dimensions/units, opening preservation, complete source-parent remapping, overwrite refusal and source-change rejection. This creates new input identity and cannot recreate missing historical private artifact bytes.

Three geometric tests establish that equal-signed fold targets are consistent with both source layer windings. They also expose an offset conflict in the restricted family of equally folded congruent layers separated by one translation: folded outer frames and unfolded side frames require different translations. At fold angle `theta`, at least one of those two normal-seam residuals is at least `d * abs(sin(theta / 2))`. Satisfying the outer frame at `d = 0.11 mm`, `theta = 1.2 rad` leaves only **0.039859 mm** perpendicular body clearance, below the **0.1 mm** contact core. Deformable configurations are outside this restricted proof; strain and actual layer-side behavior must be measured rather than declaring the entire cuff impossible.

Coupled target fidelity, fixed-partition/timestep/refinement controls, actual turning through the source opening, corner/allowance treatment and sleeve attachment remain required. Binding, collar/stand operations, gathers, localized closures, six interfacing roles and all 24 fabric instances remain full-shirt gates. The application remains placement inspection.

### Corrected baseline and unchanged-ramp hold

The [new ledger](../research/3d-cuff-hold-results.json) and [measured figure](../research/3d-cuff-hold-results.png) compare two freshly captured Linux ARM controls using the explicit HashGrid profile. Both preserve the same source geometry, material, 20 registrations, 24 hinges, 0.5 mm seam target, 1.2 rad fold target and numerical code. The hold changes only schedule/provenance and execution duration/budgets. Its first 67 accepted states, through the original 64 ms ramp, have bit-identical positions and velocities.

The baseline completes with **67 accepted steps / three rejected attempts**, consuming **66.727 worker CPU seconds**. The 256 ms control completes with **306 accepted steps / 50 rejected attempts**, consuming **350.596 worker CPU seconds**. Independent replay verifies all **373 states**, **126,686 exact contact leaves**, **35,808 exact nondegeneracy leaves** and **125,848 required candidate occurrences**. Maximum reconstructed force residuals are `9.8411e-7 N` and `9.9934e-7 N`, below the unchanged `1e-6 N` numerical threshold.

During the additional 192 ms hold, maximum vertex speed falls from **1.50883 to 0.0193617 m/s**. Shell angles change from **49.35–74.11°** to **55.67–78.36°**, while facing angles barely change from **0.096–38.59°** to **0.094–39.07°**, against **68.75°** targets. Maximum seam-vector target error changes from **0.002665 to 0.002642 mm**; final edge-length ratios remain approximately **0.9837–1.0268**. This supports investigating coupled constraints rather than assuming duration alone resolves target fidelity. It does not prove equilibrium or global incompatibility. No physical damping is added; implicit integration dissipates motion, and timestep/refinement/material acceptance remains open. Both reports retain `accepted: false`.

### Seam-law comparison and crease-frame ambiguity

A third control uses the same 256 ms canonical source, placement, schedule, material, contact settings and numerical code, changing only normal-offset sewing to scalar-distance sewing. It completes **266 accepted steps / ten rejected attempts** in **129.927 worker CPU seconds**. Replay verifies all states, **128,759 exact contact leaves**, **25,536 triangle leaves** and **128,074 required candidate occurrences**; maximum reconstructed residual is `9.9622e-7 N`. The [comparison figure](../research/3d-cuff-hold-results-seams.png) and expanded [ledger](../research/3d-cuff-hold-results.json) retain separate residual definitions and adaptive partitions.

Facing angles improve to **29.03–62.04°** and shell angles to **62.87–75.09°**; final edge ratios are **0.98543–1.01553**. Maximum speed is **0.20305 m/s**, substantially above the normal-offset hold's value. Scalar distance error is **0.007064 mm**, which must not be compared as the same residual as a normal-offset vector error. Different adaptive partitions and residual motion prevent an equilibrium or equivalent-trajectory claim. Scalar distance does not enforce layer side or seam orientation; it is a diagnostic, not an accepted replacement.

Source review then identifies an incidental binding choice: `combine_cuff_controls` selected the first containing child triangle after crease subdivision and used it as the facing normal frame. At the outer crease, body and allowance children represent the same anchor but have different normals during folding. The five outer registrations and two duplicated corner rows select allowance-side frames by child order; adjacent side registrations use body frames. Facing tip vertices have zero seam-anchor coefficients but receive frame rotation reactions. The repaired extractor requires an explicit `--crease-frame-region body|allowance` declaration when both sides contain a crease anchor. It derives the body halfspace from consistent off-crease source registrations, rejects mixed/absent side evidence or mismatched source anchor pairs, and selects a source-parent child containing the unchanged negative-anchor support. Canonical primitive ordering makes frame selection independent of supplied child enumeration. Policy, source parent/child identities and limitations are recorded. The allowance variant reproduces every historical frame; the body variant changes only rows 9–15. Every other physics field, including exact anchor coefficients, remains identical. Independent constructive regressions show that both layers can attain 1.2 rad with unchanged source metrics and essentially zero body-frame seam error while retaining endpoint clearance. This is an endpoint construction, not a dynamic/path result or turned cuff. Symmetrizing layer reactions alone would retain the former region ambiguity. The final focused Linux run passes 18 tests across source generation, sequence binding, geometric compatibility and independent frame mechanics. The dynamic controls and limits are recorded below.

### Body-frame contact-buffer sensitivity

Three fresh body-frame holds preserve identical source geometry, targets, duration, 0.1 mm minimum separation, 10,000 Pa pressure and captured numerical source. Only the contact activation distance changes between them. The [separate evidence ledger](../research/3d-cuff-frame-contact-results.json) and [figure](../research/3d-cuff-frame-contact-results.png) record exact differences from the earlier allowance-frame control, including the input-binding helper revision. Changing activation changes the contact force law; it is not an equivalent replay, physical calibration or garment acceptance.

| Activation buffer | Accepted / rejected integration steps | Worker CPU seconds | Final shell angles | Final facing angles | Final maximum speed |
| --- | --- | --- | --- | --- | --- |
| 1 mm | 288 / 32 | 204.756 | 43.18–80.36° | 3.23–51.61° | 0.02955 m/s |
| 0.1 mm | 256 / 0 | 55.125 | 67.52–68.41° | 67.37–68.33° | 0.01382 m/s |
| 0.05 mm | 257 / 1 | 57.589 | 67.50–68.41° | 67.52–68.41° | 0.01765 m/s |

The prescribed target is 68.75°. Both narrow-buffer controls approach it without resetting rest geometry, weakening the 0.1 mm separation requirement, changing force-residual tolerances or omitting candidates. Their final edge ratios are **0.9997843–1.0001516** and **0.9999644–1.0000802**, respectively. Maximum seam-vector target errors are approximately **0.0000449 mm** and **0.0000443 mm**. These are numerical measurements in an uncalibrated model, not physical precision claims. Finite actuation compliance and passive bending remain; motion is nonzero and adaptive histories differ.

Replay verifies all **801 saved states** across the three body-frame controls, **497,588 exact contact leaves**, **76,896 nondegeneracy leaves** and **497,242 required candidate occurrences**. The narrow-buffer runs have maximum reconstructed residuals `8.4472e-7 N` and `7.4876e-7 N`. Their first replay attempts terminated without writing a verified artifact under the old 100/105-second soft/hard CPU limits. The replay CLI now admits an explicit bounded `--cpu-limit-seconds` (default 100, maximum 3,600), records requested/actual CPU, and retains exclusive artifact creation. Independent AST comparison confirms verification calculations are unchanged; three budget regressions pass on macOS and Linux. Reruns with a 600-second CPU budget and external 900-second wall bound complete in approximately 122.40 and 129.29 process CPU seconds, with peak resident memory about 332 MiB. The earlier failures remain recorded diagnostics.

A nominal half-timestep control also completes and passes full saved-path replay for 513 states and 404,784 exact contact-proof leaves. All 256 positive coarse saved fractions match fine saved fractions exactly; comparison uses unchanged world coordinates without interpolation. Final maximum position difference is 0.173797 mm and hinge difference 0.028385°; trajectory maxima are 0.298685 mm and 0.106456°. This is a two-resolution temporal sensitivity check, not asymptotic convergence, spatial refinement, a valid material/contact profile, actual cuff turning or sleeve attachment. Parallel source-layer folding remains a diagnostic rather than the source construction sequence.

The [separate temporal ledger](../research/3d-cuff-temporal-results.json), [figure](../research/3d-cuff-temporal-results.png) and [reproduction details](../research/3d-solver-feasibility.md#two-timestep-sensitivity) preserve the exact adaptive partitions, proof budgets, per-sample differences and remaining motion. Reported trajectory maxima cover common saved times only. Seven portable analytic/malformed-input reporter tests pass. Independent review verifies every comparison and all saved hinge angles with a separate edge-plane construction; neither reporting nor review upgrades numerical proof to physical acceptance.

### Cuff construction input boundary

The [source construction phase work](../research/3d-solver-feasibility.md#source-cuff-construction-phases--2026-09-23) addresses the inspection graph's atomic cuff-attachment ordering. A separately versioned, explicitly selected research policy retains the original source graph and records four dependency refinements. It reserves the outer attachment opening until after turning and final allowance treatment; gathering remains incomplete during the intermediate stages. Both opening-binding prerequisites and original final memberships remain present.

The new five-fabric generator preserves the sleeve, both cuff layers and both binding strips, including all 40 sampled star rows. Source code and parent-input drift independently reject before output publication. Unequal 297/220 mm source arcs remain unchanged. Interfacing, sleeve/body operations, localized closures, material sides, turning passage and seam coverage are explicit unresolved obligations. The phase plan and generated unit are declarative research inputs, not numerical execution or proof of physical construction. Their strict source/plan correspondence validator cannot grant acceptance.

Eighteen focused tests pass on macOS and pinned Linux ARM (ten phase tests, eight generator tests). Independent review caught omission of binding stitch-down/opening-apex obligations; the final plan preserves the source instructions and requires those finishing steps before binding completion. Turning explicitly requires allowance/corner treatment without undeclared trimming. Separate tamper cases reject early shell activation, premature gather/perimeter completion, an altered opening or fabricated verified gates. These are checks of the declaration; no runtime milestone has been completed.

### Material-gripper integration review

The [new virtual controls](../research/3d-solver-feasibility.md#compliant-source-material-controls--2026-09-23) add an external force term to free cloth, with source-triangle provenance and explicit activation/release. Review checks the force/energy coupling against a separate dense backward-Euler quadratic, combined sewing/fold derivatives, external impulse, zero activation and rejection of fixed supports. Malicious optimizer endpoints cannot bypass triangle or contact-path guards. Rejected adaptive attempts retain original schedule fractions and cannot publish work; per-transition energy and impulse are recorded before accepted journal outcomes.

Input review found that NumPy coercion could erase mixed Boolean values in captured rest coordinates and triangle indices. The binder now validates raw JSON types and geometry bounds first. A second configuration defect admitted a subdivision/depth combination beyond the schedule's exact dyadic domain; adaptive and CLI validation reject it before attempts. Interrupted supervisor recovery drops a worker aggregate that could describe a different accepted prefix. The current replay independently reconstructs prefix totals and compares gripper-work sums exactly, including values too small for a fixed absolute tolerance.

Arithmetic review reproduced false convergence from three opposing high-stiffness pulls: separately rounded force products summed to zero while original-input evaluation gave a residual above 0.001 N. The same case reversed the sign of tiny directional energy changes. A second independent review found lost torque inside a cross product, before final accumulation. The repaired term uses exact binary-input evaluation on bounded source support when force/work/torque cancellation or coefficient rounding requires it. The trigger includes cross-product operand magnitudes. A separate 150-digit Decimal suite checks original-input force, torque, weighted-anchor and coefficient cancellation, and both directional signs. Exact fallback is common for balanced controls; no unchanged-performance claim is made.

Independent gripper replay uses its own source validation, control sampling and exact binary-input quadratic/work formulas without importing captured solver modules. It checks source bindings, initial stored energy, scheduled controls, force/reaction reports, parameter-work decomposition, velocity continuity and free-cloth external momentum. It returns a separate nodal gradient for the enclosing residual check. Other force terms remain numerical and use captured implementations; this does not prove an exact full-cloth force balance or execute a construction phase.

The focused Linux run passes **61 gripper tests**, including seven supervised capture/replay cases and nine semantic tampering attacks. A separate retained [contact-control ledger](../research/3d-material-gripper-contact-results.json) has eight saved states. Independent review recomputes every gap/speed and 150-digit Decimal parameter-work total, checks all captured and current-verifier hashes, and independently enumerates swept bounds. The 216 primitive comparisons require zero collision-core candidates; sixteen triangle leaves pass. The fixture exercises contact near its activation boundary and release, not narrow collision-core proofs or any garment operation.

### Pending-seam primitive review

The [activation primitives](../research/3d-solver-feasibility.md#pending-seam-mechanics--2026-09-23) retain source rows while weighting all sewing terms, including the direct initializer and material-frame reactions. Independent dense backward-Euler checks cover mixed active/pending vector rows; complex-step checks cover weighted normal curvature. Pending coincident scalar anchors and unused frame geometry are skipped, while malicious optimizer endpoints still fail the enclosing triangle/contact guards. Active error diagnostics are unweighted. The adaptive boundary rejects raw weights rather than silently running an unrecorded control.

Independent 180-digit Decimal cases check cancelling target/activation work, tiny distance/normal motion, zero-weight overflow and coupled sewing/fold/gripper accounting. Omitted-activation energy dictionaries match the committed legacy implementation exactly in all three modes. Exact rational work is conditional on sampled binary64 lengths/normals and does not certify exact geometric normalization. Review also found positive extended-precision input could become zero during float64 conversion; activation now rejects that loss instead of silently making a row pending.

An additional schedule counterexample used a zero initial weight and the smallest positive binary64 final weight. Midpoint interpolation rounded a mathematically positive activation to zero. Sampling now rejects that underflow without clipping or promoting the weight; exact endpoints and representable tiny values remain supported.

At the primitive checkpoint, source-member binding, adaptive capture, CLI integration and independent trajectory replay remained required. The next section records their integration; the standalone schedule's row IDs alone still supply no such proof.

### Captured-seam integration review

The [captured controls](../research/3d-solver-feasibility.md#captured-seam-controls--2026-09-23) preserve the complete source bundle and canonical row order, reject source/member/frame/compliance drift and distinguish generic JSON correspondence from rederived cuff pattern geometry. The cuff validator reconstructs all forty source rows and eight selectors, including the interleaved gather members, and retains the original phase obligations. It rejects repaired-hash source/partition attacks and incompatible coefficient reproduction. Snapshot namespace tests cover both layouts, incomplete dependency trees, changed engine bytes and cached modules from a different root. Nested capture paths reject traversal and symbolic-link redirection.

Adaptive review reproduced mutation of the target array after a valid solve and a Boolean masquerading as reported zero activation. Fresh sampling and strict raw-type validation now reject both before accepted work is published. Tests cover original-fraction retries, rollback, journal ordering, exceptions, interruptions and combined fold/gripper controls. Initial energy is recorded explicitly rather than imposing a zero-residual placement assumption.

Independent replay initially disagreed with scalar work on varied barycentric anchors because ordinary multiply/add did not reproduce SciPy's fused sparse accumulation. Explicit binary64 fused multiply-add matches the tested macOS/Linux ARM samples; sixty varied mode/weight fixtures retain exact rounded rational work comparisons. Backend differences remain a possible fail-closed rejection, not an implicit portability promise. Cuff pattern rederivation and other cloth terms retain their separately stated verification boundaries.

The first end-to-end selection ran four tests and retained four failures across its cases: replay compared the solver's rounded residual-energy diagnostic to the exact original-input potential used for work accounting. A combined seam/gripper capture independently exposed the same near-cancellation distinction. The verifier now reconstructs the rounded diagnostic arithmetic separately while retaining every exact helper energy/work comparison. All-pending energy diagnostics require exact zero, including rejection of a forged smallest subnormal value. The failed CLI log remains in ignored `.planning/solver/sewing-capture-cli-sep23-v1.log`; no numerical acceptance threshold was relaxed.

### Current verification boundary

The [fixed-timestep binding comparison](../research/3d-solver-feasibility.md#fixed-timestep-binding-time-control--2026-09-23) retains identical source geometry, target paths and physical settings while declaring a fourfold duration increase. Independent review checks strict timing policy/run agreement, historical input admission, complete 64/256 grids, all non-timing source identities and separate whole-passive/final-sixteen-millisecond ranges. It repaired Boolean fraction/time values masquerading as numerical zero/one. Both new 256-step runs replay without rejected attempts; improved tracking still leaves relative motion. The full pinned Linux numerical/supervision suite passes **716 tests in 288.967 seconds**. No settled-pose, convergence or construction status is granted.

The first source binding controls retain all five fabrics and forty rows. Both matched runs replay 64 steps, including exact sampled seam-path bounds and candidate coverage. Independent local-frame and complete-surface observations reveal a 4.485° overshoot for a 3° command and continued final motion; the [evidence interpretation](../research/3d-solver-feasibility.md#first-source-binding-motion--2026-09-23) rejects a settled pose or completed construction claim. Neither run has active saved contact energy, so these controls do not establish wrapping contact response.

Reporter review found and repaired incomplete matching of actual recipes/placement versus their declarations, missing verifier identity checks, unchecked angle-dependent clearance/contraction claims, and an anchor-force sum that omitted the exact barycentric weight sum for near-normalized inputs. The final reporter compares the complete normalized source with a narrow, reconstructed angle-dependent exception list, checks all current verifier identities and full-run state continuity, and preserves weighted nodal-force accounting. The actual three dyadic anchors sum exactly one; the force-report defect did not change the retained control results. The source-frame helper and exact saved-state surface helper were independently cross-reviewed; these observations retain their endpoint-only scope.

The final sampled seam-path checkpoint passes **660 full pinned Linux numerical/supervision tests in 315.871 seconds**. This includes the preceding namespace hardening, eleven new sweep tests, sixteen independent adversarial cases and three CLI tests. All twenty-seven focused sweep tests pass on macOS as well. The full log is ignored `.planning/solver/sewing-path-full-numerical-sep23-v1.log`; focused counts overlap it. Documentation and whitespace checks pass. No application, browser, build or deployment work is included.

The optional sampled scalar seam-path gate uses original binary64 coefficients and endpoint coordinates as exact rationals, independently of rounded sparse diagnostics. Review covers endpoint-passing interior failures, changing targets, the lower-bound sign crossing, exact equality and one-ULP rejection, active collapse, pending/engagement semantics, tiny activation, cancellation and source-control knot boundaries. The reference is affine interpolation of independently reconstructed endpoint targets; it does not change backward-Euler work accounting or supply spatial stitching evidence.

Independent review caught an admission error before release: comparing a converted float directly to `np.int64(2**53+1)` or its unsigned counterpart could itself round the integer, hiding the loss. Admission now compares integer values without NumPy promotion and rejects a nonrepresentable input before constructing its rational proof. The regression retains the one-unit target-error counterexample. Captured target bounds already excluded that particular large target; the repair makes the standalone exact-input claim valid as well.

Captured seam-control integration passes **628 full Linux numerical/supervision tests in 268.099 seconds**, before the final namespace hardening. The four CLI, seven independent combined-control/capture and two actual-cuff focused tests all pass and overlap that discovery. The complete log is ignored `.planning/solver/sewing-capture-full-numerical-sep23-v1.log`. The all-pending source cuff verifies source/capture integration, not construction execution.

Final review reproduced a standalone namespace fallback through a dangling adjacent `services` link. The enclosing capture verifier already rejected directory links, but the source resolver now independently rejects live or dangling `services`/`engine` links before layout selection or resolution. Eight new subcases cover both snapshot layouts, including a complete parent fallback that must remain unused. The final **14 source-binding tests pass on Linux ARM in 9.021 seconds and macOS in 6.133 seconds**. Both actual-cuff capture/replay tests pass again on the final tree in **16.119 seconds**, with the log retained as `sewing-capture-cuff-final-sep23-v1.log`. These focused checks follow the broad run and do not change its historical count. Documentation and whitespace checks pass; application and deployment scope remain unchanged.

The seam-activation primitive checkpoint passes **565 full Linux numerical/supervision tests in 201.509 seconds**, including 51 new primitive, schedule, direct-integration and energy cases. Its log is retained in ignored `.planning/solver/sewing-activation-full-numerical-sep23-v1.log`. Fifteen primitive and nine schedule tests also pass on macOS, and the 48-test energy selection passes on both platforms. These focused selections overlap the full Linux discovery.

For the material-gripper checkpoint, the final full Linux numerical/supervision discovery passes **514 tests in 203.919 seconds**. The initial 514-test run retained two failures in unrestricted mock solvers, which fabricated a gripper recipe when their new optional attribute was read. Explicitly declaring vector mode and absent fold/gripper recipes fixes those two test fixtures; their underflow and journal-byte-budget assertions remain unchanged. The focused 24-test journal run and final full rerun pass. No numerical tolerance or admission rule was relaxed. Both logs remain in ignored `.planning/solver/gripper-full-numerical-sep23-v1.log` and `gripper-full-numerical-sep23-v2.log`.

Before the Linux broad-phase discovery, the macOS ARM run selected **365 tests: 364 passed and one was skipped** because this platform has no extended-precision `longdouble`. This included source generation and the numerical regressions then present. It excluded the Linux-only `test_solver_process_budget`, `test_solver_contact_continuation_cli` and `test_solver_fold_continuation_cli` modules, plus the malformed-staging CLI case and two native-interruption/supervisor-recovery cases in the contact-preflight/journal tests. Their `/proc` and `prctl` controls are retained; they are not replaced with weaker macOS substitutes.

After the correction, the full Linux numerical/supervision discovery passes **427 tests in 180.822 seconds**, including native lifecycle and CLI checks. **Five later independent candidate/replay review tests** pass separately, as do **six source-generator tests**. Earlier focused counts overlap this discovery and must not be summed into another full-suite result. Both corrected cuff captures pass the strengthened replay; the earlier defective capture rejects. The initial failed 415-test run is retained as a failure. Documentation and whitespace checks pass. Production engine, application, browser and build suites have not been rerun for these offline research changes; no application code changed.

## Independently executed checks

- All six final allowance-cloth tests passed. These include every real shirt template, cut-domain triangle coverage, source reconstruction, interior stitch samples and segments, and mutations of source coverage and derived metadata.
- Independently transformed all 18 real templates by each of 37°, 90° and 179°, with translation `(111, -77)` mm. All 54 cases passed the same independent coverage and embedding oracle at a 60 mm mesh setting. These tests used ordinary tessellation, not optional quality refinement.
- A panel-level hole mutation rejected. A hypothetical `slits` property is ignored; no current pattern slit schema was found. Explicit slit support remains a future semantic gate, and these meshes must not be described as supporting slit topology.
- All seven final v2 contact-filter tests passed in the pinned Newton research runtime, including the dynamic layered probe. Separate physical layers and disconnected patches retain contact candidates; distant same-sheet primitives remain outside the exclusion maps; symmetry, transformation invariance, primitive validation and bounded search reject cases pass. A near-corner regression preserves long primitives as contact candidates.

The test named `test_local_neighborhood_and_remote_fold_candidate_maps` checks exclusion-map membership only. It creates a folded coordinate array but does not run it through Newton. The static coincident-layer test likewise verifies candidate preservation. A separate dynamic one-step test now verifies finite results and increased mean layer separation along Z with contact enabled, while the disabled-contact control preserves its initial 0.5 mm separation. This establishes a contact response for that synthetic fixture, not collision-free cloth or full-garment acceptance.

## Findings and independent rechecks

The initial runtime validator accepted reconstructing source weights whose support crossed a cut-contour concavity. The concrete `front_left` attack used cut vertices `(0, 1, 3)`, whose support extended approximately 113.426 mm² outside the source polygon. The final validator rejects both the original-vertex mutation at vertex 44 and an interior mutation at vertex 97, the latter specifically through support containment. Duplicate supports and changed original cut-vertex identities also reject.

The initial validator also accepted reversed triangle winding. The final recheck rejects that mutation and a `solverReady: true` classification mutation. The validator additionally checks used vertices, oriented edge incidence and interior free boundaries. A changed stitch source fraction rejects. An edge ending at `10^12` and a Boolean interval index both reject before range expansion; generated and validated stitch samples have global caps. The final runtime validator was included in a repeated 54-case transformed-panel check.

## Source and contact boundaries

Allowance geometry is explicitly derived from `draft.cutLine`, not relabeled as seam-line geometry. Per-vertex weights reconstruct cut-contour coordinates. Stitch paths reconstruct the original seam-edge polylines; per-triangle segment splitting permits future weighted constraints without substituting nearest vertices. The module retains `solverReady: false`: binding wraps, turning, folds, material bulk and weighted-constraint execution remain absent.

The contact filters use immutable rest-edge path distance, not current spatial proximity. Triangle and edge validation prohibits crossing physical instance IDs. This prevents coincident shell/facing pieces from becoming each other's excluded neighbors, and disconnected patches cannot be joined merely because their rest coordinates coincide. Newton's installed SolverVBD interface accepts the vertex-to-triangle and edge-to-edge exclusion maps used by the harness.

The original v1 endpoint-neighborhood method was rejected because a nearby endpoint exempted an entire long primitive, including distant cloth. Historical v1 numerical outputs must not be treated as acceptance. The final `same-instance-whole-primitive-rest-geodesic-v2` profile requires all triangle corners to be local to the query vertex and every cross-edge endpoint pair to be local before exclusion. Long source edges are also conservatively retained. Close primitive interiors can still remain candidates when endpoints are distant. A stationary-piece improvement therefore cannot certify safe self-contact during folding. Full dynamic contact and independent collision-oracle gates remain required.

No blocker was found in these narrowly labeled source-embedding and research-filter paths by the checks above. This is not release approval, full-shirt assembly acceptance, or an assertion that the garment simulation succeeds.

## Coupled IPC contact and placement admission · 2026-09-18

Reviewed the separate `solver_ipc_contact.py` adapter, optional `GlobalSewingSolver` coupling, energy accounting and `solver_contact_preflight.py`. The pinned research dependency is `ipctk==1.6.0`, installed only in the ignored numerical environment through `scripts/solver-contact.requirements.txt`. Its installed toolkit license is MIT; this is not a complete wheel-dependency redistribution review or production dependency approval.

Independent tests check all coordinate derivatives, vertex-major gradient ordering, zero net contact force, symmetric positive-semidefinite search curvature, cache isolation, squared-distance clearance comparisons, coplanar and transverse intersection rejection, and endpoint-safe tunneling. Review caught nonmanifold vertex admission and inconsistent shared-edge winding; both now reject before native mesh construction. Toolkit minimum-distance results are squared distances, despite the installed docstring's wording; the adapter compares them with squared minimum separation.

The direct solver includes contact energy, forces and a projected contact Hessian in the coupled solve. A CCD bound limits each search direction before its candidate energy is evaluated; both the optimizer segment and the original physical-step segment must pass. Independent integration attacks verify that contact forces cannot be omitted from convergence, both paths independently reject, invalid bounds fail closed, and zero or unrepresentably small steps cannot report success. Basic dynamic controls verify repulsion, incoming layers remaining separated, momentum, pinned reactions and mechanical-energy bookkeeping. These checks use the unchanged `1e-6 N` stationarity threshold.

Contact rest topology must match the solver's ordered triangles, and per-face metric/area checks use an explicitly reported `64 * float32 epsilon` tolerance for the model's stored rest data. Initial rigid placement may differ from rest coordinates. A stretched rest mesh rejects; source metrics are never reset to assembled geometry.

Independent inspection of `fold-barrier-shirt-v1/canonical.json` finds **1,511 intersecting nonadjacent pairs** in the initial 2,244-vertex, 3,512-triangle placement. Toolkit intersection detection independently returns true and the minimum active candidate distance is zero. Largest grouped overlaps include 222 collar-fall/stand pairs per layer, 121 back-left/right pairs, 117 frill-left/right pairs and 97 front-left/right pairs. This is a starting-state failure, not only a late nonlinear fold. The reproducible preflight preserves source/input digests, returns `contactAdmissible: false`, and exits with status 2.

The IPC adapter makes no seam exclusions. The old zero-distance sewing targets conflict with positive layer separation at coincident targets; turning, sewing through thickness and layer orientation need explicit semantics. Do not hide these conflicts with whole-piece exclusions or enable contact on the intersecting placement. Contact candidates exclude incident primitives; this implementation does not certify adjacent-fold thickness or triangle nondegeneracy throughout a motion. The optional local hinge guard remains a separate, limited safeguard.

All reports retain `accepted: false`. No application simulation, body interaction, material calibration, garment release or deployment is established by this review.

A further parameter review reproduced overflow/underflow through huge/tiny positive contact distances. The adapter now validates representable squared separation, activation interval and barrier scale before native construction. Five extreme-scale regression subcases reject.

The actual-source cuff shell/facing CCD control has 40 vertices, 48 triangles, unchanged source rest geometry, 2 mm initial rigid separation and opposing 0.25 m/s velocities. The default toolkit CCD budget hits the declared 120-second CPU limit before dynamics; an earlier exit 137 has no verified termination cause. A separate initial-motion control with 10,000 TightInclusion iterations, unchanged `1e-6` tolerance and `0.8` conservative rescaling returns a safe step bound of `0.8125` in 0.58 seconds. The experimental adapter captures these explicit settings in profile v2. The toolkit can retry a near-zero impact without an iteration cap, so an external process budget remains mandatory. No faster approximate CCD substitute is used.

The final source-bound cuff control completes **four of four steps in 1.155 seconds**, with maximum force residual `7.881e-7 N`. Minimum nonadjacent clearance over the recorded steps is **1.08783 mm**. Every step has zero intersections under both the toolkit and independent surface oracle, passes its physical CCD path, and retains identical material rest poses, areas and contact source coordinates. Maximum absolute total-momentum component is `6.87e-18 kg m/s`; this is numerical conservation evidence, not physical precision. The unchecked incoming motion would cross. A within-shell vertex-vector constraint holds its original offset; it is explicitly a diagnostic stabilizer, not an assembly seam.

The reviewer independently checked every saved source digest against both snapshots and current files, and verified all per-step rest, momentum, oracle and CCD fields. An aligned multi-triangle regression additionally verifies the capped bound against analytical first-contact time. Source snapshots, extracted input and states remain under `.planning/solver/ipc-source-layers-v4`; the [durable ledger](../research/3d-contact-results.json) retains the successful control and earlier failures. Neither four collision steps nor an unsewn cuff pair establishes shirt assembly or settled drape.

Final verification: **200 numerical tests, 11 global-harness tests and 104 application tests pass**. Typecheck, documentation and whitespace checks pass. The final contact source snapshots and all four cuff states were independently verified; full-shirt placement remains rejected. Browser/build suites were not repeated because no frontend, production engine or application runtime changed. Work remains isolated and undeployed.

## Rigid staging continuation · 2026-09-18

The optional preflight now supports fixed-plane rigid translation of all physical fabric instances. Independent review ran the ten staging tests and 50 seeded randomized four-piece arrangements, checking them through IPC and the independent discrete surface oracle. Inputs remain unchanged, per-piece edges are preserved and centroid preservation passes within `1e-10 m`. This validates the static construction only; no assembly path, turning, layer ordering or drape acceptance follows.

Review found three malformed-input failures in the new seam diagnostics: negative/out-of-instance local indices silently selected another piece; a malformed constraint container escaped structured reporting; and NaN coefficients contaminated the subsequent rejection report. The fixed code validates bounded containers, distinct local indices and finite normalized anchor coefficients before computing statistics. Independent CLI rechecks confirm that all four reproductions exit 2, retain a serializable rejected report and create no staged artifact. These cases are now automated subprocess regressions.

The reviewed full-shirt preflight passes static contact admission with zero detected intersections and unchanged source dimensions. Maximum seam gap remains approximately 492 mm, so it is not an assembled shirt. The active research notes record its inter-instance clearance separately from much smaller within-instance candidate distances. The exact-source snapshot is retained under `.planning/solver/rigid-shirt-staging-reviewed`.

Final applicable checks for this continuation: **212 numerical tests and 104 application tests pass**, plus typecheck. Browser/build and production engine suites are not repeated because only offline research scripts and documentation change. The alternative sparse ordering was reviewed for linear-system correctness but regressed measured performance and was reverted; it is not part of the patch.

A second independent review rechecked all staging source/artifact hashes, proper-rigid source residuals, seam gaps and both surface oracles. It also checked the rejected 300-evaluation state: zero detected intersections, maximum edge ratio **2.611754**, force residual **0.00615923 N**, and minimum active-candidate distance **0.232170 mm**. A suggested independent-rest-deformation CLI regression now verifies that nonrigid source mismatch leaves a rejected report and no staged artifact.

Contact diagnosis independently reproduces artificial in-plane forces at flat source rest. Adjacency filtering loses real local-fold contact; OGC has discontinuous rebuilt-set energy on parallel layers; area-weighted improved-max has consistent scalar derivatives but an indefinite assembled `CLAMP` metric and altered stiffness units. All three remain rejected as drop-in changes. The existing contact adapter is unchanged. Measured counterexamples and source digests are retained in the [continuation ledger](../research/3d-rigid-staging-results.json); they identify further work, not an accepted new contact profile.

## Area-contact and temporal subdivision review · 2026-09-18

The subsequent optional area-contact profile explicitly resolves units and guards signed search curvature, but does not claim to resolve all intrinsic mesh contact. Independent analytical checks verify layer energy and resultant normal force for three pressures and three activation ranges. Rebuilt collision-set finite differences verify gradients and raw Hessian products; rigid covariance, zero resultant force, immutable rest coordinates and invalid numerical scales are covered. Energy-mode configuration is read-only after native potential construction. The legacy default is retained.

Independent refinement tests demonstrate remaining false rest forces even with area weighting. A narrower activation range removes them on this particular source mesh without excluding collision candidates. Tests also demonstrate that further refinement can reactivate them or violate the fixed minimum separation. Crossing layers initially outside activation still receive a conservative CCD bound, and a local fan fold retains force and continuous collision rejection. No broad adjacency, instance or seam exclusion was introduced.

Both assembled metrics are guarded. An indefinite primary is rejected in favor of the projected-membrane fallback; a bounded inertia shift guards that fallback if the signed contact Hessian makes it indefinite. Independent tests verify unchanged objective descent, rejected unrepairable metrics, correct free-particle inertia, and unchanged legacy behavior. The earlier primary-shifting trial is retained as rejected evidence.

Independent artifact checks on the two failed 1%-closure narrow-range trials and the converged 0.01%-closure control verify input/placement digests, all 24 instances and 2,244 vertices, unchanged source snapshots, zero intersections under both endpoint oracles and passing recorded physical paths. These endpoint checks do not upgrade failed stationarity. The small control moves vertices at most 0.0431684 mm and has no active contact; it cannot establish assembled or settled cloth. A separate narrow-range incoming-layer stress probe exhausts its 60-second CPU budget before a result and remains a failed resource control.

The independent adaptive-wrapper review checks rollback even when a rejected solver call mutates its input, exact dyadic duration accounting, linear targets over original time, numerical retries, resource stops, false convergence flags, and attempt/depth bounds. Positions, velocities and completed time advance only after a finite converged candidate meets the unchanged residual threshold. Reports expose incomplete progress and retain all failed attempts. This is numerical continuation infrastructure, not garment acceptance.

## Physical-path and durable continuation closure · 2026-09-19

The optional `temporal-separation-tight-inclusion` profile separates the conservative optimizer proposal from physical-path certification. Tight Inclusion and the static swept-plane filter still size proposals. Both optimizer-segment and physical-chord guards, endpoint intersection validation and hinge sweeps remain mandatory. Default profiles and the 1e-6 N stationarity threshold are unchanged.

Independent review found no remaining blocking numerical false acceptance in the declared nonincident-primitive affine-motion scope. An extended-precision time collapsing onto a binary64 endpoint exposed a helper defect; non-float64-exact time and minimum-distance scalars now reject with a retained regression. Verification includes 53 distinct tests, 2,845 exact certificates from 6,400 arithmetic inputs, and zero omissions among 2,393 expected broad-phase overlaps. These are separate evidence categories. The reviewer independently verified the blocked step's 7,084 leaves and force residual. Its pre-scalar-fix snapshot differs from final source, but the final helper reproduces that entire proof identically.

The final-source 1% target-reduction interval completes over 1/240 second in eight substeps with no rejections. Parent-side saved-state replay, without rerunning the optimizer, verifies source/input/state hashes, both endpoint intersection oracles, velocities, stationarity, hinge sweeps and exact rational support/coverage for 60,250 leaves. Maximum residual is 1.326702969e-7 N; final contact energy is 3.398987285e-6 J, peak force component 0.905930012 N and maximum seam gap 486.715308 mm. This eight-state replay is separate from the reviewer's narrower blocked-step audit. See the [source-bound ledger](../research/3d-physical-path-results.json).

The lost-rejection blocker is resolved by immutable, fsynced, hash-chained start/outcome records with exact interval/dt/lineage and last accepted-state hashes. The real accepted → rejected → native-stalled retry regression retains all outcomes and 0.005 seconds of accepted progress after process-group termination. Tagged nonfinite diagnostics cannot be accepted. Recovery rejects invalid records, orphan states and boolean-as-time confusion, checks provenance and final state, and reserves evidence capacity. The 77 journal/supervision/CLI cases are included in **334 numerical tests**; **104 application tests** and typecheck also pass.

The bare helper is not an initial-intersection oracle: adapter endpoint validation must remain. Contact certificates do not prove swept triangle nondegeneracy, incident-material thickness or body contact. Native broad-phase resource use still requires external CPU/wall/address-space supervision. Host loss, supervisor SIGKILL and filesystem failure cannot guarantee new terminal publication. Discretization-dependent intrinsic contact and finite-thickness sewing incompatibility remain open. None of this grants garment, drape, physical-fit or deployment acceptance; production remains untouched.
